# Conduit Phase 5 — Meetings & the record timeline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Meetings as a first-class record with attendees and follow-up tasks, feeding the existing events timeline — and mail folded into that timeline without weakening Phase 4.2/4.3 privacy. Released as v0.9.0.

**Architecture:** Two new tables (`meetings`, `meeting_attendees`) in migration 0008 plus two new FK columns and three verbs on the existing `events` table. A meetings service shaped like notes.ts/files.ts, a fifth rail tab, and one privacy-critical change to `listEvents`: mail rows are pointers, joined at read time through the composed 4.2 visibility + 4.3 hide predicates. Spec: `docs/superpowers/specs/2026-08-27-conduit-phase-5-meetings-design.md` — the authority; its Decisions table and the mail-privacy matrix are the completeness checklists.

**Tech Stack:** No new dependencies (TipTap, sanitize-html, Drizzle, TanStack Query all present). All Phase 2-4.3 conventions BINDING: every command on the dev server via `CONDUIT_REMOTE_DIR=/home/chris/conduit-phase4 ./scripts/remote.sh '<cmd>'` from the worktree root (vitest from repo root); ASCII-only sources with `String.fromCharCode` for non-ASCII fixtures; NodeNext `.js` import extensions in `packages/api` only, none in `packages/web`; NEVER `drizzle-kit push`; shipped migrations immutable, unshipped ones editable in place with the journal timestamp preserved and `conduit_test` converged via psql; `db:generate` drift-free at every task end; the independent-EXISTS fold discipline; comment discipline (state only what was measured, never narrate changes); targeted `git add` only; archive-not-delete everywhere. Suite at start: 1586 unit + 36 integration + 57 e2e, green. Branch `worktree-phase-5-meetings` from the v0.8.0 manifest commit (99edcf4).

**File structure (locked in here, referenced by task):**

| File | Responsibility |
|---|---|
| `packages/api/src/db/schema.ts` | `meetings`, `meetingAttendees` tables; `events.meetingId`/`events.mailThreadId`; widened verb CHECK |
| `packages/api/drizzle/0008_*.sql` (+ snapshot, journal) | migration 0008 |
| `packages/api/src/db/schema.test.ts` | the `withPreMigrationDatabase("0008")` drill + constraint tests |
| `packages/shared/src/index.ts` | `meetingSchema`, `meetingAttendeeSchema`, create/update inputs, list filters, widened `eventVerbSchema`, `meetingId`/`mailThreadId` on `eventSchema` |
| `packages/api/src/services/meetings.ts` (new) | meetings CRUD, attendee set replacement, list filters incl. contact-attendance widening, event emission |
| `packages/api/src/services/meetings.test.ts` (new) | that service's tests |
| `packages/api/src/routes/meetings.ts` (new) | `/api/meetings` routes incl. `POST /:id/tasks` |
| `packages/api/src/routes/index.ts` | register the meetings routes |
| `packages/api/src/services/timeline.ts` | actor-aware `listEvents`; the mail-pointer join + visibility/hide filtering |
| `packages/api/src/services/timeline.test.ts` | the mail-privacy matrix |
| `packages/api/src/services/mail-ingest.ts` | emit `mail_received`/`mail_sent` with the per-thread-per-direction-per-day throttle |
| `packages/web/src/components/rail/meetings.tsx` (new) | the Meetings rail tab: list, log form, meeting view, follow-up tasks |
| `packages/web/src/components/rail/rail.tsx` | mount the fifth tab |
| `packages/web/src/components/rail/timeline.tsx` | badges + rendering for `met`/`mail_received`/`mail_sent` |
| `packages/web/src/queries.ts` | meetings hooks + the events hook's actor-scoped keys |
| `e2e/meetings.spec.ts` (new) | the meeting journey + the two-user privacy leg |

---

### Task 1: Migration 0008 + shared contracts

**Files:** `packages/api/src/db/schema.ts`, `packages/api/drizzle/0008_*.sql` + `meta/`, `packages/api/src/db/schema.test.ts`, `packages/shared/src/index.ts`, `packages/shared/src/index.test.ts`

`meetings`: `id` uuid PK defaultRandom, `title` text NOT NULL, `occurredAt` timestamptz NOT NULL, `durationMinutes` integer NULL, `notes` text NULL, `ownerUserId` uuid NOT NULL FK users, `companyId`/`contactId`/`dealId`/`projectId` uuid NULL FKs, `archivedAt` timestamptz NULL, `createdAt`/`updatedAt` timestamptz NOT NULL defaultNow. CHECK `meetings_has_link`: `company_id IS NOT NULL OR contact_id IS NOT NULL OR deal_id IS NOT NULL OR project_id IS NOT NULL` (the spec's reachability decision — comment WHY: no top-level list exists, so an unlinked meeting is unreachable the moment it saves). Follow the `events` multi-FK model deliberately, NOT notes' exactly-one — comment that a deal meeting legitimately carries its company too.

`meeting_attendees`: `id` uuid PK, `meetingId` uuid NOT NULL FK meetings, `contactId` uuid NULL FK contacts, `userId` uuid NULL FK users, `guestName` text NULL. CHECK `meeting_attendees_exactly_one` (notes' `notes_exactly_one_entity` is the pattern to mirror — read it first). Two partial unique indexes: `(meeting_id, contact_id) WHERE contact_id IS NOT NULL` and `(meeting_id, user_id) WHERE user_id IS NOT NULL`; guests deliberately not deduped (comment why: free text, two people can share a first name).

`events`: add `meetingId` uuid NULL FK meetings and `mailThreadId` uuid NULL FK mail_threads; widen the `events_verb_valid` CHECK with `met`, `mail_sent`, `mail_received`. Existing rows untouched; no backfill anywhere in 0008.

FK style: plain NO ACTION, matching every FK in this schema (archive-not-delete; nothing is ever deleted — see the 0007 DONE block's note).

Drill via the existing `withPreMigrationDatabase("0008", ...)` helper (0007's drill is the freshest example — copy its shape, including the premise-pinning assertion: assert BOTH new tables ABSENT before `migrate()` runs). Seed a pre-0008 company + event through raw SQL naming only pre-0008 columns; after migrate, assert the tables exist, the pre-existing event survived with NULL `meeting_id`/`mail_thread_id`, and that the widened CHECK accepts `met` while still rejecting a bogus verb. Constraint tests on the shared handle: both CHECKs, both partial uniques, the FKs.

Shared: `meetingSchema` (all columns, ISO strings for timestamps), `meetingAttendeeSchema` (discriminated by which id is set — model it as three nullable fields plus a superRefine enforcing exactly-one, mirroring how the DB CHECK reads, and comment that the refine and the CHECK are the same rule in two places), `meetingCreateInputSchema` (title, occurredAt, durationMinutes?, notes?, the four record links, attendees array) with a superRefine enforcing at-least-one-link (the CHECK's twin), `meetingUpdateInputSchema` (all optional; attendees replace the set when present), `meetingListFiltersSchema` (the record filters + `archived` tri-state, matching `threadListFiltersSchema`'s house shape), and widen `eventVerbSchema` + add `meetingId`/`mailThreadId` to `eventSchema`. `db:generate` drift-free; `conduit_test` converged by plain migrate. ~12 tests.

> **DONE** (commit e69e37e; spec review passed with two comment fixes in the micro-round). Suite 1610 unit (+24 on the 1586 baseline) + 36 skipped; typecheck clean on five projects; db:generate drift-free; conduit_test converged by plain migrate.
>
> **As built.** schema.ts: `meetings` :628-676 (CHECK `meetings_has_link` :675, spelled `num_nonnulls(...) >= 1` so it reads as one family with notes'/files' `exactlyOne` at :249 — reachability WHY at :657-668), `meetingAttendees` :700-712 (CHECK :711, no `created_at` by design :687-693), `events.meetingId`/`.mailThreadId` :301/:310 (both `references((): AnyPgColumn => ...)` — forward-declared targets, the `pipelines.projectId` device), verb CHECK :319-322. Migration `0008_long_strong_guy.sql`: generated 1-40, hand-written index block 41-88 (0004's `mail_accounts_user_email_active_unique` terms), the meetings-side deferral 89-102, no-backfill rationale 103-111. Shared (line refs current as of the quality round): `eventVerbSchema` :124, event pointers :143/:153 (required-and-nullable, deliberately not optional), `attendeeIdentityFields` :1337, `attendeeIdentityKeys` :1359 + `attendeeExactlyOne` :1362 (the key list is DERIVED from the field record, so a fourth attendee kind cannot be missed by the predicate -- that failure would be permissive, zod accepting what the CHECK then raises as a 500), `meetingAttendeeSchema` :1369, `meetingAttendeeInputSchema` :1382, `meetingAtLeastOneLink` :1408 (EXPORTED, `taskDatesPaired`'s precedent), `meetingSchema` :1414 (attendees :1437), `meetingInputShape` :1445 (NOT exported), create :1458, update :1479, list filters :1494.
>
> **The drill** (schema.test.ts:951) pins its premise three ways before migrate: both tables absent, both events columns absent, and `met` REJECTED by the 0007 CHECK. Post-migrate: tables exist, the pre-existing event survived with both pointers NULL, new rows reference pre-migration parents, `met` inserts, `convened` still rejects, and all three hand-written indexes are present in `pg_indexes` with their COLUMN LIST, order, uniqueness and predicate asserted — that last block specifically because they live in no snapshot, so only a from-the-files migration proves the .sql still carries them. The column-list assertion is the quality round's fix: the reviewer mutated the shipped contact index to `("contact_id") WHERE contact_id IS NOT NULL` — silently turning "a contact can be on this meeting once" into "a contact can be on ONE MEETING EVER" — and the original UNIQUE+predicate assertion passed. Re-verified after the fix: the same mutation now fails the drill. Reviewer-verified by mutation that the drill applies the shipped SQL (removing `met` from the migration's CHECK fails it).
>
> **HANDOFFS for Task 2 (several correct the plan's own text — read before starting):**
> - **The sanitizer: the plan's Task 2 text is WRONG.** notes.ts has NO sanitizer — `notes.body` is plain text rendered with `whitespace-pre-wrap` from a plain Textarea. Import `sanitizeMailHtml` from `services/mail-content.ts:203` (the system's one shared profile, already reused for `email_templates.body_html`), and copy `mail-templates.ts:50`'s `sanitizeBody` behaviour of rejecting a body that sanitizes to empty.
> - **`meetingSchema` carries a REQUIRED `attendees` array** (:1437, not in the spec's data model). Every meeting-returning path — create, get, update, archive, unarchive, and every `listMeetings` row — must hydrate attendees or the client's `parseWith` throws. There is no read path that wants the bare row.
> - **`updateMeeting` must re-assert `meetingAtLeastOneLink` against the MERGED row** and raise a 4xx: the refine deliberately does not ride `meetingUpdateInputSchema` (:1468-1479, `taskDatesPaired`'s precedent — a patch sees one snapshot, never the stored row), so clearing the last link would otherwise surface as a 500 from the CHECK. The predicate is EXPORTED for exactly this (shared:1408, `taskDatesPaired`'s own reason for being exported) and its parameter type takes a merge result unchanged — import it, never write a second copy.
> - `notes` is `nullableString` = `.min(1).nullable()` — `""` is a 400, not an empty note; normalise blank to `null` in both service and form. `durationMinutes` is `.int().positive()` — `0`/fractions are 400s and the DB carries no matching CHECK (zod is the only gate, on `contacts.emails`' precedent); a blank field must omit or send `null`.
> - `meetingListFiltersSchema.archived` is a plain boolean; the wire tri-state coercion is the ROUTE's job (see `routes/companies.ts`'s `listQuerySchema` comment on why `z.coerce.boolean()` cannot be used). `true` = archived only; absent/false = live only.
> - Attendees replace as a SET, never patched row-by-row (`meetingAttendeeInputSchema` carries no `id`, :1376-1381). Duplicate-attendee inserts surface as Postgres `23505` — remap to `ConflictError` the way mail-accounts.ts does for its partial unique.
> - **Indexes: what 0008 now ships, and what it deliberately does not** (quality round; measured at 50k meetings / 150k attendees, warm — this supersedes the first draft of this bullet, which was wrong on both the capability and the precedent). SHIPPED: the two partial uniques now lead with the IDENTITY column — `(contact_id, meeting_id) WHERE contact_id IS NOT NULL` and the user twin — because uniqueness of a pair is order-independent, so the flip enforces the identical rule at identical cost while making each index probe-usable by that column alone; the contact-attendance `EXISTS` runs through it as a hashed SubPlan with an Index Only Scan (305 buffers / 1.08ms). Also shipped: `meeting_attendees(meeting_id)`, the hydration index every meeting-returning path needs and the ONLY index a guest row appears in (attendee load 1,392 buffers / ~18ms parallel seq scan → 6 buffers; a 50-row page becomes a bitmap index scan) — `mail_attachments_message_id_idx` / `mail_messages_thread_id_idx` are the precedent. DEFERRED, with numbers: the meetings-side scan is the real cost centre at 50k (782 buffers / 4.3ms; 43 buffers / 0.10ms with a candidate `(company_id, occurred_at DESC, id DESC)`), because the `OR` in the contact-attendance arm prevents any index driving the outer side — but this deployment holds hundreds of meetings, where that scan is sub-millisecond. RE-MEASURE TRIGGER: on the order of 10k meetings, re-EXPLAIN the four record filters and add the record-FK/keyset indexes then. The structural twin to copy is `mail_threads` (four record FKs, keyset-paginated, per-record tabs — 0004 ships all four record-FK indexes plus a keyset index), NOT notes/files/events, which are unpaginated whole-record loads. 0008 stays UNSHIPPED and editable in place (journal timestamp preserved, `conduit_test` converged via psql — and note the shared-handle constraint tests run against the OLD database until that convergence happens).
> - Both event pointers on one row are legal at schema level; nothing in Phase 5 writes such a row (pinned as column shape, not use case, schema.test.ts:1212-1218).
>
> **COORDINATOR RULINGS for Task 2 (settled in the quality round — do not re-litigate):**
> - **Ordering is `occurred_at DESC, id DESC`, not `created_at`.** A meetings list is about when the meeting HAPPENED; ordering by creation would put "logged today about last month" above "logged yesterday about tomorrow", and `occurredAt` is deliberately free in both directions. Per `pagination.ts`'s own naming rule this needs a THIRD cursor type + decoder (`OccurredAtCursor`) so a cursor minted by one list cannot decode against another — that cost is accepted; say in a comment why the third type exists.
> - **Notes that sanitize away to empty become `null`, NOT a 409.** `mail-templates.ts`'s 409 is justified by its `bodyHtml` being non-nullable `.min(1)` ("a template that renders as nothing is not a template"); `meetings.notes` is nullable and a meeting with no notes is completely valid. Reuse the sanitizer profile, not that error code.
> - **`taskCount` on `meetingSchema`, full tasks on a detail payload.** The rail's list rows need only a count (spec); follow the mail house pattern — `mailThreadSchema` vs `mailThreadDetailSchema` — so `getMeeting` returns `{ meeting, tasks }` while list rows carry `taskCount`. NOTE for Tasks 3/4: that count queries `events.meeting_id`, a brand-new UNINDEXED column on the fastest-growing table, which Task 4 is about to add mail rows to — MEASURE it when Task 3 writes it.
> - **Hydration shape:** one `inArray(meetingAttendees.meetingId, page.map((r) => r.id))` per page, regrouped in JS (`mail-threads.ts`'s `loadAggregates` shape) — never `Promise.all(rows.map(getMeeting))`. `MeetingAttendee` carries `meetingId` for exactly this.
> - **Merge idiom:** `patch.companyId !== undefined`, not `"companyId" in patch`.
> - The service must `trim()` `guestName`: `"   "` currently passes both `.min(1)` and the CHECK.

### Task 2: The meetings service + routes

**Files:** `packages/api/src/services/meetings.ts` (new), `packages/api/src/services/meetings.test.ts` (new), `packages/api/src/routes/meetings.ts` (new), `packages/api/src/routes/index.ts`, `packages/api/src/routes/routes.test.ts`

Read `packages/api/src/services/notes.ts` and `files.ts` first — this service copies their shape (service owns the rules, routes own the wire). `createMeeting`, `getMeeting`, `updateMeeting`, `archiveMeeting`, `unarchiveMeeting`, `listMeetings`.

- `notes` HTML sanitized with `sanitizeMailHtml` from `services/mail-content.ts:203` — the system's one shared sanitizer profile. (This corrects an error in this plan's first draft: notes.ts has NO sanitizer; `notes.body` is plain text. See the Task 1 DONE block.) Copy `mail-templates.ts:50`'s `sanitizeBody` behaviour of rejecting a body that sanitizes to empty.
- Attendees are replaced as a set on update when `attendees` is present, left alone when absent. One statement per side (delete-not-in + insert-missing, or delete-all + insert — measure nothing, just keep it obviously correct and comment the choice).
- `occurredAt` accepts past OR future (the spec's line: logging what happened and noting what is arranged are the same act).
- `listMeetings(db, opts)`: filters `companyId`/`dealId`/`projectId` are plain FK matches; **`contactId` matches `meetings.contact_id = C OR EXISTS an attendee row for C`** — this is the decision that makes attendance a real link, and it is the reviewer's first grep. Cursor pagination + the `archived` arm as **`services/companies.ts:112`'s `listCompanies`** does (reuse `pagination.ts`) — NOT notes/files, which have neither cursor pagination nor an archived arm (correcting this plan's first draft). Order by `occurred_at DESC, id DESC` per the coordinator ruling in the Task 1 DONE block, which needs its own cursor type and decoder.
- Event emission through the existing pattern (read how notes.ts emits `note_added`): create emits `met` carrying the meeting's four record FKs AND `meetingId`; archive/unarchive emit the existing `archived`/`unarchived` verbs with `meetingId`. No content in any payload beyond what notes already puts there.
- Routes: `GET /api/meetings` (filters), `POST /api/meetings`, `GET/PATCH /api/meetings/:id`, `POST /api/meetings/:id/archive`, `POST /api/meetings/:id/unarchive`. `requireUser` on every route, as every other route file does; owner is the actor. Register in `routes/index.ts`.

Tests (~18): CRUD; both CHECKs surfacing as clean 4xx not 500s (at-least-one-link is caught by the zod refine BEFORE the DB — pin both layers); attendee set replacement (add, remove, replace-with-empty, the partial-unique rejection of a duplicate contact); the contact-attendance widening (a meeting linked to company A with contact C as attendee appears under `?contactId=C` — and does NOT appear for an unrelated contact); archived filtering; sanitization (a `<script>` in notes does not survive); event emission (a `met` event lands on every linked record with `meetingId` set); route-level authz + shape.

> **DONE** (commits 94e5a44 + 1ce704e + this round; spec review passed compliant-with-issues, quality review with-fixes, both rounds landed). Suite 1649 unit (+38 on the 1611 baseline) + 36 skipped; typecheck clean on five projects; db:generate drift-free; `conduit_test` converged via psql for 0008's new index.
>
> **As built.** `services/meetings.ts`: hydration helpers :80 (`loadAttendees`) / :105 (`taskCreatedFromMeeting`, the ONE criterion) / :122 (`loadTaskCounts`) / :141 (`loadMeetingTasks`); `meetingEventValues` :329 (the four record FKs + `meetingId` + `payload`); `createMeeting` :345, `getMeeting` :408, `updateMeeting` :433, `setArchived` :534, `listMeetings` :597 (contact-attendance arm inside it). `routes/meetings.ts` all six routes; registered `routes/index.ts:106`. `services/pagination.ts`: `OccurredAtCursor` :28 + `decodeOccurredAtCursor` :57. Shared: `taskCount` on `meetingSchema` :1454, `meetingDetailSchema` :1465, `guestName` trimmed before `.min(1)` :1346. `services/timeline.ts:81`: the read-time widening. `services/errors.ts:42`: `DuplicateAttendeeError`; mapped at `routes/helpers.ts:65`.
>
> **Rulings settled during the two reviews, all implemented:**
> - **Timeline payload.** All three verbs a meeting emits stamp `payload: { title }` (`meetings.ts:329`). Event rows are append-only history and the web timeline renders exclusively from the payload, so a row written blank renders blank forever. `archived`/`unarchived` carry the title too, unlike every other record type's archive event: a company's archive entry sits on that company's own timeline, while a meeting's lands on a record that may hold dozens.
> - **Attendance widens the timeline at READ time** (`timeline.ts:81`), never by fanning out event rows: one `met` row per meeting, so a company timeline cannot sprout one entry per attendee, and "attendance is a real link" stays ONE rule expressed the same way in `listMeetings` and here.
> - **The widened arm reaches the meeting's own lifecycle rows only** (`AND task_id IS NULL`). Once Task 3 stamps `meeting_id` onto a follow-up task's `created` event, that row's `meeting_id` is PROVENANCE, not subject: the task reaches timelines through its own links, and an attendee seeing task activity she is not on is noise.
> - **`IN`, not a correlated `EXISTS`,** in that arm — measured, not stylistic: a correlated subquery is not parallel-safe, so it serialised the scan of the one table with no record-FK indexes (162.7ms vs 44.8ms at 300k events, identical rows). `listMeetings`' own arm correlates on `meetings.id`, cannot be rewritten this way, and needs nothing (312 buffers / 2.9ms at 20k meetings).
> - **No `updated` event on a meeting edit** (`updateMeeting`'s header): the spec names exactly three emissions, and correcting the notes on a meeting already in the story is not a second meeting. `actorId` is taken and unused for call-site symmetry; pinned by a test.
> - **Two distinguishable 409s.** `DuplicateAttendeeError extends ConflictError` (`errors.ts:42`) purely so the route answers `duplicate_attendee` rather than `conflict` — `MailCredentialDecryptError`'s precedent. Task 5's form branches on the code: a duplicate attendee is one row to fix, an emptied last link is a different section. Both codes pinned at the route.
> - Existence checks on links and attendee identities are **existence-only** (deals.ts's rule, not notes.ts's): an archived company can still receive a meeting that happened. Pinned.
>
> **0008 gained a FOURTH hand-written index** (`events_meeting_id_idx`, partial on `meeting_id IS NOT NULL`, migration line 118) and 0008 stays UNSHIPPED; `conduit_test` was converged with the same DDL via psql, journal timestamp untouched. MEASURED at 300,005 events / 50,000 meetings / 150,000 attendees, warm: list-page task count 42.7ms / 4,243 buffers -> **0.120ms / 103**; detail task list 39.6ms / 4,258 -> **0.046ms / 19**; index 1,552 kB against a 33 MB table (50,005 of 300,005 rows). The cost was present tense, not deferred: scanning 300k events to return nothing costs the same as scanning them to return five. The drill (schema.test.ts:1077) asserts its column list AND its partial predicate; removing the CREATE INDEX fails it.
>
> **RE-MEASURE TRIGGERS now split by what each scan is over** (migration lines 126 and 144): the deferred meetings-side keyset index triggers on ~10k MEETINGS; the timeline's record filters (unindexable while `events` ships no record-FK indexes) trigger on ~1M EVENTS (~150ms, extrapolated from the measured 300k figure).
>
> **RULING for Task 3 — widen `createTask`, never append a second `created` row.** `taskCreatedFromMeeting` (`meetings.ts:105`) requires the task's OWN creation event to carry `meeting_id`, and `createTask` writes that event inside its own transaction with no hook. Task 3 must therefore widen `createTask` with an origin parameter (`meetingId`) that rides its existing event insert — atomic, one `created` row. Appending a second `created` row would satisfy the count while putting two creation entries for one task on that task's own timeline. This is a ruling, not an option.
>
> **KNOWN LIMITATION (D6, accepted).** Event links drift on update: adding `contactId` to a meeting after creation leaves the `met` row's `contact_id` null while a later `archived` row carries it, so a newly-linked record's timeline can show "archived" with no "met". Inherent to append-only logs — a fix means rewriting history or fanning out rows, both worse. The read-time widening does not affect it (that arm is about attendance, not record links).
>
> **FOR TASK 5.** A renamed meeting leaves the OLD title in payloads already written (correctly — history is append-only), so a timeline entry can disagree with the Meetings tab. Render the payload title as what was true then; the entry links to the meeting for the current one. Wire contract: list rows parse `meetingSchema` (both `attendees` and `taskCount` required), detail parses `meetingDetailSchema`; query params are snake_case (`company_id`/`contact_id`/`deal_id`/`project_id`/`archived=true|false`/`cursor`/`limit`); blank notes send `null` or omit, never `""`; `durationMinutes` omits or sends `null`, never `0`; SSE keys are `[["meetings"], ["meeting", id], ["events"]]`.
>
> **FOR TASK 4.** `timeline.ts:77` carries a TASK 4 MUST PRESERVE THIS ARM marker: the mail filtering is an ADDITIONAL predicate over the same WHERE, and replacing the contact arm's OR with a plain equality silently empties an attendee-only contact's timeline. Test-backed at timeline.test.ts:74.
>
> **NOTED, deliberately not changed:** `hydrate` runs after commit, so two concurrent PATCHes can return writer A's title with writer B's attendees (a read skew in the echo, never in storage); the redundant `mustGet` in archive/unarchive (faithful to companies.ts's house shape); a shape-valid cursor carrying an unparseable timestamp 500s rather than 400s (pre-existing in every list in this codebase, not a meetings bug).

### Task 3: Follow-up tasks from a meeting

**Files:** `packages/api/src/services/meetings.ts`, `packages/api/src/services/meetings.test.ts`, `packages/api/src/routes/meetings.ts`, `packages/api/src/routes/routes.test.ts`, `packages/shared/src/index.ts`

`POST /api/meetings/:id/tasks` → `createMeetingTask(db, actorId, meetingId, input)`, which:
- loads the meeting (404 if absent/archived — archived meetings do not sprout new work; comment the ruling),
- calls the EXISTING `createTask` service (never a second task-creation path — the compactor, scheduling and dependency rules live there),
- copies the meeting's four record links onto the task, with any link the caller supplies taking precedence (comment: the meeting's links are defaults, not a cage),
- records `meetingId` on the task's originating event payload so the timeline entry can read "created from this meeting".

The input schema is the existing task-create input MINUS the record links (they are inherited) — derive it from the existing schema rather than restating fields, so a future task field cannot drift.

`getMeeting` gains the tasks it produced by querying `events` for rows with this `meetingId` and a task-creation verb, then loading those tasks — deliberately NOT a new column on `tasks`: the link already exists in the event payload, and a denormalised column would be a second source of truth for the same fact. Comment that reasoning where the query lives. ~8 tests: links inherited; caller override wins; archived meeting refuses; the created task obeys every existing task rule (assert one scheduling behaviour to prove `createTask` really ran); the meeting view lists its tasks.

> **DONE** (commits 1eb71c1 + b2f636e + this round; spec review compliant-with-issues, quality review with-fixes, NO correctness defect in either -- every finding was documentation or coverage, and this block plus the SSE pin are the fixes). Suite 1660 unit (+11 on the 1649 baseline) + 36 skipped; typecheck clean on five projects; db:generate drift-free; 0008 unchanged apart from a comment, so no schema change and no `conduit_test` convergence.
>
> **As built.** `services/meetings.ts`: `createMeetingTask` :608 -- loads the meeting, refuses an archived one, merges the four links, calls `createTask` with the origin, then publishes the MEETING's own SSE keys (`taskCount` on every list row and the detail payload's task list both just changed, and `publishTaskHint` carries only task keys). `services/tasks.ts`: `TaskOrigin` :174, `createTask(db, actorId, input, origin?)` :176, and the stamp `meetingId: origin?.meetingId ?? null` :244 -- inside createTask's EXISTING event insert, in its own transaction, so exactly ONE `created` row exists and it is the row `taskCreatedFromMeeting` (meetings.ts:105) reads. The parameter is optional and purely additive: of the 126 call sites, exactly one passes an origin (createMeetingTask); the other 125 -- `routes/tasks.ts:54` plus 124 in tests -- are unedited. Shared: `meetingTaskCreateInputSchema` :1561 = `taskInputShape.omit({the four record links}).refine(taskDatesPaired)`, DERIVED so a task field added later reaches this affordance instead of silently dropping off it. Route: `routes/meetings.ts:110`, 201 with the TASK (the shape POST /api/tasks already answers with). Tests: `meetings.test.ts:579-793` (8) and `routes.test.ts:1856/1885/1909` (3), plus the new route in the 401 sweep. The 8th is the SSE pin at :748 -- the quality review deleted `publishMeetingHint` and all 129 other tests stayed green, so the key set this block hands Task 5 is now asserted rather than described (mutation-verified: exactly that one test fails without the publish).
>
> **The ruling is mutation-proof, which was its point.** A second `created` row appended after the fact fails `meetings.test.ts:694` (the task's own timeline carries exactly one entry) and `:580` (exactly one row names the meeting) -- while `taskCount` alone would still read 1, because Task 2's COUNT(DISTINCT task_id) absorbs the duplicate. The count was never going to catch it.
>
> **D2 -- THIS TASK'S TEXT ABOVE IS SUPERSEDED on the refusal codes.** It says "404 if absent/archived". Shipped: **404** for an unknown meeting, **409 `archived`** for an archived one -- the same code and body PATCH-on-archived already answers (`routes/helpers.ts`'s ArchivedError arm), because an archived meeting exists and is still readable: it has been filed away, and filing it away means it does not sprout new work. Unarchive it and the identical request succeeds, which is exactly what that code tells a client. Pinned at both layers (`meetings.test.ts:638`, `routes.test.ts:1909`).
>
> **D4 -- the meeting id rides the `events.meeting_id` COLUMN, not the payload.** This task's text (and the spec's Follow-up tasks section) say "payload"; spec Amendment 2 records the correction and the reasoning -- the column is what the criterion filters, what 0008 indexes, and what already reaches the client as `Event.meetingId`. FOR TASK 5: the follow-up `created` row ships `payload: {}`, so its timeline entry can never render the meeting's TITLE -- render generic wording and link through `Event.meetingId`. (A task's creation entry does not name the task either; this is the existing convention, not a Phase 5 gap.)
>
> **D3 -- TASK 5 REQUIREMENT, not a note: the archived-project dead end.** The wire shape omits the four record links, so a client cannot send `projectId: null`. A meeting whose `project_id` points at an ARCHIVED project therefore has NO reachable follow-up path over HTTP: `createTask`'s `assertProjectActive` refuses every request, and its 409 names the PROJECT -- indistinguishable by `error` code from the archived-MEETING 409, both being `archived`. Task 5 must disable the "Add task" affordance with a stated reason when the meeting's project link is archived, rather than let a user hit a 409 that nothing on screen explains. (The service merge itself is fine: a direct caller passes `projectId: null` and the task is created. It is the wire that cannot say it, and shipping it that way is the accepted cost of the omission this plan mandates -- non-strict parsing being universal in this codebase.) Second condition of that same ruling: **Task 5's form must not send record links at all**, since they are stripped silently -- a form that sent them would look like it worked and quietly do something else.
>
> **KNOWN LIMITATION (accepted; coordinator ruling, out of scope here).** `createTask`'s event stamps `task_id`, `project_id` and `company_id` (direct or via project) -- never `contact_id` or `deal_id`. That is identical for `POST /api/tasks` and predates Phase 5, but Phase 5 newly EXPOSES an asymmetry: a meeting stamps all four record FKs on its `met` row, so a deal's timeline shows the meeting and never the follow-up it spawned -- even though that task carries the deal link and appears against the deal everywhere else. The task's own links are correct and tested; only the event's are narrow. A fix belongs in a phase that gives `events`' record-FK columns a purpose on task rows, not here.
>
> **Wire contract for Task 5.** `POST /api/meetings/:id/tasks`, body = the task-create input MINUS the four record links (title required; description/type/assigneeUserId/startDate/dueDate/progressPct/parentTaskId optional; the two dates paired or both absent). Every reachable outcome: **201** parsing `taskSchema`; **400 `validation`** for an unpaired date; **404** unknown meeting; **409 `archived`** for an archived meeting, and for an archived inherited project (see D3); **409 `conflict`** when `parentTaskId` names a task in another project or one that is itself a subtask (`assertParentValid`, Phase 3's one-level rule) -- `parentTaskId` IS on the wire, since the omit drops only the four record links, so **the form should not offer it unless it is prepared to render that 409**; and **500** for an unknown `assigneeUserId`, which `createTask` alone among the id fields does not pre-check, so the FK fires and `mapDomainError` rethrows. That 500 is pre-existing and identical on `POST /api/tasks` -- recorded here so Task 5 is not surprised by it, NOT to be fixed in this task. On success the server publishes the task keys AND `[["meetings"], ["meeting", id], ["events"]]`, so the rail's count, the detail task list and the timeline all refresh from the one write.
>
> **Reviewer's Task 5 findings (quality review), worth having before the tab is built:**
> - **D3 needs no new API field.** `GET /api/projects/:id` returns archived projects and `useProject` keys on `["project", id]`, so TanStack dedupes a rail list down to one fetch per distinct project: read `archivedAt` there and disable Add-task with a stated reason. This supersedes any thought of adding a `projectArchived` flag to `meetingSchema`.
> - **`taskSchema` is the right 201 body** -- `sse.tsx` maps published keys straight onto `invalidateQueries`, so the refreshed count arrives by the ordinary route. Even so, the mutation's `onSuccess` should invalidate `["meeting", id]` and `["meetings"]` locally (the house pattern at `queries.ts:337`/`:780`) rather than depend on the SSE hop and its 100ms coalesce.
> - If a second origin kind ever lands, `TaskOrigin` becomes a discriminated union, never `{meetingId?, mailThreadId?}` -- that shape permits both-or-neither. Stated on the interface itself (`tasks.ts:174`).
>
> **RE-MEASURED, with the follow-up rows Task 2 could only measure the absence of** (300,005 events of which 110,005 carry a meeting -- 50,005 lifecycle rows plus 60,000 follow-up creation rows -- over 50,000 meetings, 150,000 attendees and 60,000 tasks, warm): the list-page count for 50 ids returning 100 matching rows is **0.198ms / 155 buffers** (bitmap index scan, 5 heap blocks) against **34.7ms / 4,477** with `events_meeting_id_idx` dropped; the detail task list for a meeting with two tasks is **0.050ms / 14** against **36.1ms / 4,485**. Both plans held their shape -- 60,000 tasks did not turn the detail query's EXISTS into a scan of `tasks`, the planner still driving the semi-join from the index into `tasks_pkey`. The index grew 1,552 kB -> 2,528 kB against a 35 MB table. Figures live in `drizzle/0008`'s index comment and both function headers; that migration's two re-measure triggers are unchanged.

### Task 4: Mail in the timeline (the privacy-critical task)

**Files:** `packages/api/src/services/mail-ingest.ts`, `packages/api/src/services/timeline.ts`, `packages/api/src/services/timeline.test.ts`, `packages/api/src/routes/events.ts`, `packages/web/src/queries.ts` (key only)

**Emission** (`mail-ingest.ts`): after a message is stored and its thread links resolved, emit `mail_received` (inbound) or `mail_sent` (outbound) carrying the thread's record FKs and `mailThreadId`. **The payload carries NO content** — no subject, no snippet, no addresses. Throttle: one event per `(thread, verb)` per calendar day (UTC — say so in the comment), enforced by an existence check on the same thread/verb/day before inserting. A thread with no record links emits nothing (there is no timeline for it to appear on).

**Read-time filtering** (`timeline.ts`): `listEvents` gains the viewer's id — every caller already has one (`routes/events.ts` has `requireUser`; check the task-drawer path too). For rows carrying `mailThreadId`:
- join `mail_threads` under the composed **Phase 4.2 record-visible predicate AND the Phase 4.3 not-hidden predicate** — import the existing helpers from `mail-threads.ts`, never re-derive them (4.2's `visibleThreads`/`visibleMessageTerm` and 4.3's `notHiddenByViewer`; read their headers for the un-aliased preconditions);
- a thread the viewer cannot see (or has hidden) → the event row is **excluded from the result entirely**, not rendered as a stub — an "activity you can't see" entry leaks both existence and timing;
- a visible thread → the response carries the subject rendered LIVE from `mail_threads.subject` (a new nullable `mailSubject` field on the event payload shape in shared — comment that it is derived-at-read, never stored).
- Exclusion happens BEFORE the limit, like every other predicate in this codebase, so a page is never short (the 4.3 pagination lesson — pin it with a test).

Tests (~20, the phase's core): the mail-privacy matrix — (owner | other user) x (private | shared account) x (unlinked | deal-linked) x (hidden | not hidden) against `listEvents`, asserting excluded-entirely vs. live-subject; **a payload-shape assertion that no mail event payload contains content** (assert the stored payload keys directly so a future field addition trips it); the throttle (second same-direction message same day emits nothing; next day emits; opposite direction same day emits); exclusion-before-limit; non-mail events unaffected by the new join (a regression guard for the whole existing timeline).

> **DONE** (commits c7555e3 + 670946d + this round; spec review COMPLIANT with no divergences at any severity -- the reviewer built seven adversarial privacy probes, including two real users at the HTTP layer, ran seven mutations trying to produce a leak, and could not, and independently confirmed this gate is byte-identical to the record Mail tab's own, so the timeline is not one degree more permissive than a surface that already shipped). Suite 1681 unit (+21 on the 1660 baseline: 18 in commit c7555e3, one time-zone pin in the spec-review round, two in the quality round -- the deal/project emission pin and the SSE `events`-key pin -- plus assertions added to the existing migration drill and privacy matrix) + 36 skipped; typecheck clean on five projects; `db:generate` drift-free; `conduit_test` converged via psql for 0008's fifth index.
>
> **As built.** `services/timeline.ts`: `toEvent` :8 (takes the derived subject as a parameter), `listEvents` :118 with the viewer POSITIONAL, mail WHERE arm :195, the gated `leftJoin` :212. `services/mail-ingest.ts`: `emitMailEvent` :636, call site :1024, the `["events"]` SSE key :1041. `routes/events.ts:30` keeps `requireUser`'s User instead of discarding it. Shared: `mailSubject` :176. Migration 0008 gained a FIFTH hand-written index (line 164) and stays UNSHIPPED. Tests: `timeline.test.ts:226` (`describe("timeline mail privacy")`), `mail-ingest.test.ts:1181` (`describe("ingestMessage: timeline entries")`).
>
> **THE PREDICATE SHAPE, AND THE THREE REASONS.** Both mail predicates ride the LEFT JOIN's ON clause -- `visibleThreads(viewerId, "record")` (4.2) and `notHiddenByViewer(viewerId)` (4.3), imported, never re-derived -- with one WHERE term written against the JOINED row (`mail_thread_id IS NULL OR mail_threads.id IS NOT NULL`) rather than repeating the rule.
> 1. **The subject cannot outlive the predicate.** `mailSubject` is selected from the same joined relation the predicate gates, so there is no arrangement of this query in which an excluded thread still hands over its subject. Every alternative renders from one place and excludes from another, with nothing tying the two together. The reviewer named this as what makes several of its attacks structurally impossible rather than merely untested.
> 2. **Exclusion is inside the statement, therefore before the LIMIT** -- no short pages, and no cursor minted from a row the viewer never saw (the 4.3 lesson).
> 3. **Parallelism**, below.
>
> **MEASURED** (300,000 events of which 30,000 carry `mail_thread_id`, over 2,000 threads / 6,000 messages / 2 PRIVATE accounts owned by two users -- 0006's narrowest configuration -- 250 hides for the viewer, one thread in four project-linked; warm, top-level Execution Time, median of four runs, on a scratch database templated from `conduit_test` and dropped after):
>
> | shape | time | plan |
> |---|---|---|
> | shipped (ON clause), unfiltered newest page -- the worst case | **56ms** / 8,087 buf | Gather Merge, 2 workers |
> | pre-Task-4 baseline, same page, no join | 46ms / 4,167 | same |
> | shipped, company filter (what a rail issues) | **35ms** / 4,474 | same |
> | pre-Task-4 baseline, company filter | 35ms / 4,167 | same |
> | uncorrelated `mail_thread_id IN (SELECT ...)` in the WHERE | 153ms | **SERIAL** |
> | correlated `EXISTS` on `events.mail_thread_id` | 779ms | **SERIAL** |
>
> The rail shape pays nothing measurable; only the unfiltered page shows the join at all. Note the inversion worth remembering: the `IN` form that RESCUED Task 2's attendance arm goes serial here, because this subquery's own correlated EXISTS terms make the hashed SubPlan parallel-restricted.
>
> **0008 GAINED A FIFTH INDEX** (`events_mail_thread_id_idx`, partial on `mail_thread_id IS NOT NULL`, migration line 164; coordinator ruling after the spec review confirmed the reasoning). Its ONLY consumer is the throttle's existence check, which runs once per ingested message INSIDE the global ingest lock -- per MESSAGE and serialised, not per page request. The reviewer verified no read path can use it: `listEvents`' only reference to the column is the `IS NULL OR ...` disjunct, which no partial index can serve, and the join drives from `events` INTO `mail_threads`' primary key. MEASURED on the same dataset: the check is **35ms / 4,077 buffers** (parallel seq scan) without it, **0.14ms / 17 buffers** with it (bitmap index scan, 15 heap blocks); 288 kB against a 32 MB table; every listEvents plan unchanged either way (55ms/38ms with, against 56ms/35ms without -- inside run-to-run noise). Same in-place mechanics as Task 2's index: journal timestamp preserved, `conduit_test` converged with the identical DDL via psql. The drill (`schema.test.ts:1089`) asserts its COLUMN LIST and its partial predicate; removing the CREATE INDEX fails it (mutation-verified).
>
> **THE VIEWER IS POSITIONAL** -- `listEvents(db, viewerId, opts)`, not a field on `ListEventsOptions`. An optional field could be forgotten at a call site and the function would still compile, answering with someone else's mail on the timeline; positional and required means the compiler names every caller. There is exactly ONE production caller, `routes/events.ts:30`, and it serves every timeline surface there is -- the record rails, a project's timeline and the task drawer are all `GET /api/events` with a different filter (`from(events)` appears in only one other place, `meetings.ts:131`, which is the follow-up-task count and touches no mail). Test callers threaded: `timeline.test.ts` (13), `meetings.test.ts` (3), `routes.test.ts` (1).
>
> **MUTATION TABLE.** Nine mutations run against this task's pins:
>
> | mutation | tests that fail |
> |---|---|
> | `visibleThreads(...)` dropped from the ON clause | **6** -- the privacy matrix; excludes another user's private mail entirely; widens for a project link and not a contact link; excludes invisible rows before the limit; leaves non-mail events exactly as they were; and mail-ingest's end-to-end bridge |
> | `notHiddenByViewer(...)` dropped | **2** -- the privacy matrix (its 8 hidden cells); drops an entry for the viewer who hid the thread and for nobody else |
> | exclusion moved out of the statement into a JS filter after `slice(0, limit)` | **1, exactly the right one** -- excludes invisible rows before the limit |
> | `payload: {}` -> `payload: { direction }` (a plausible, NON-content addition) | **2** -- the payload key-set assertion and its outbound twin. The important negative: a value check for today's field names would have missed it |
> | UTC boundary -> session-local `date_trunc('day', now())` | **1** -- the new time-zone pin (O1 below). Before that pin existed, this mutation left the whole suite green |
> | `CREATE INDEX events_mail_thread_id_idx` removed from 0008 | **1** -- the migration drill |
>
> **O1 -- THE UTC BOUNDARY IS NOW TEST-ENFORCED** (`mail-ingest.test.ts:1322`), and it was the review's one green mutation: dev and CI both run at `Etc/UTC`, where the correct expression and a session-local one coincide. The pin opens its OWN database handle carrying `?options=-c TimeZone=Pacific/Kiritimati` -- +14, the largest offset in the tz database, where the local day boundary never coincides with UTC's -- because a `SET TIME ZONE` on the shared two-connection pool cannot be pinned to the connection a later query lands on. Which SIDE the local boundary falls on depends on the time of day the suite runs (14 hours early before 10:00 UTC, 10 hours late after), so the test asserts BOTH probes: an entry half an hour after UTC midnight must suppress, and one half an hour before it must not. The two instants come from `Date`'s own UTC accessors, never from a copy of the service's SQL, which would have been mutated alongside it and proved nothing.
>
> **O2 -- the pointer test's fixture now carries five markers of different KINDS** (`mail-ingest.test.ts:1201`): subject, body phrase, sender address, sender DISPLAY NAME and a distinctive RECIPIENT. The original three would have caught a new column holding subject, body or sender, but not one holding only a display name or a recipient -- the fixture's From had no display name and its To was the account's own address. The payload key-set assertion remains the airtight half; this closes the column-side gap.
>
> **O3 -- A THROTTLE/LINK-DRIFT INTERACTION THAT D6 DOES NOT COVER.** Record links are a snapshot at emission (D6), and the throttle adds to that: if a thread gains a deal link AFTER that day's entry was emitted, the rest of the day's messages are throttled away, so the newly-linked record's timeline shows nothing of that conversation until the next UTC day. This follows from the throttle exactly as specified -- the alternative is re-emitting on every link change, which would double entries on timelines that already had them -- and it is a delay of at most a day on a record that has just been linked, not a permanent gap. Recorded in `emitMailEvent`'s header so it is a known consequence rather than a surprise.
>
> **O4 -- `queries.ts` NEEDED NO EDIT, despite this plan listing it.** `["events"]` already prefix-matches every events query key, so the SSE hop invalidates them all. And the cache keys stay un-actor-scoped deliberately: `["mail-threads", params]`, `["mail-thread", id]` and `["mail-unread"]` have been per-viewer-filtered since v0.7.0 with no viewer segment, because the cache is per browser session and identity comes from the reverse proxy's `Ynh-User` header, which cannot change within one. Adding a viewer segment here alone would be a new convention in a single place. TASK 5 SHOULD NOT RE-OPEN THIS; if it disagrees, all of the mail keys change together or none do.
>
> **O5 -- THE PARALLELISM CLAIM RESTS ON THE MEASUREMENT ABOVE, NOT ON A LAW,** and the comment now says so. The reviewer could not reproduce the difference on an EMPTY dev database, where every shape plans serially and this one's own scan carries a hashed SubPlan that is itself parallel-restricted. Row-count statistics plausibly explain that, and it does not refute the figures -- but anyone re-running this needs the dataset to see what was seen.
>
> **O6 -- THE VISIBILITY SCOPE IS HARD-FIXED TO `"record"`,** which is right today: a mail event only exists when its thread carries record links, and every listEvents filter is one of those FKs, so every timeline this function serves is a record's. REVISIT the day an unfiltered "all activity" timeline lands -- that surface would be a mailbox view, and "record" would make it one degree more permissive than the inbox beside it. Flagged on `listEvents` itself.
>
> **FOR TASK 5 (web).**
> - `Event` gains `mailSubject: string | null` (shared :176), required-and-nullable like the two pointers. Non-null EXACTLY when `mailThreadId` is -- an invisible thread yields no row at all -- so the tab never needs an "unavailable" state. Render it as the entry's label; link to the thread through `mailThreadId`.
> - Unlike a meeting's payload title, this value CANNOT go stale: threads take their subject once from their first message and never rewrite it, and it is read live at request time.
> - `summarize()` still has three cases to gain (`met`/`mail_received`/`mail_sent`); the badge letters landed in 5271ea6 per Amendment 1.
> - SSE: ingest publishes `["events"]` ONLY when a timeline entry was actually written -- a throttled message or an unlinked thread publishes the three mail keys as before, so a busy thread does not make the rails refetch a response that cannot have changed. Pinned by a test.
>
> **FOR TASK 6 (e2e).** The two-user privacy leg behaves exactly as the spec's Rollout describes: A's private, merely auto-linked thread produces an entry for A and NO ROW AT ALL for B. Assert it against a loaded timeline carrying a sentinel entry B can see (`timeline.test.ts:397` is the unit-level shape to mirror). The release notes must carry the no-backfill line -- the timeline starts telling the mail story from upgrade forward, and absent history is not a bug.
>
> ---
>
> **QUALITY REVIEW ROUND (this round).** Verdict: with fixes, both HIGHs closed here rather than carried to Task 5. The reviewer built a scratch database at **50x the threads and 5x the mail rows** of the measurement above and established the property the design actually rests on: the planner puts the **Nested Loop Left Join ABOVE the Gather Merge**, so `mail_threads` is probed roughly once per row the page RETURNS (~61 loops for a 51-row page) rather than once per row scanned. **The join cost is O(limit), not O(table)** -- rail 23.0ms against a 20.4ms baseline, throttle 0.027ms on the new index. It also caught that the 35ms rail figure above was measured on the CHEAP half (company 1's threads all live in the other user's mailbox, so the visibility SubPlan returned rows=0 every loop and the subject-rendering path never ran); re-run on an owned company, the claim still holds.
>
> **THE STATISTICS CLIFF, now recorded in `timeline.ts`'s header and named in 0008's read-side re-measure trigger.** What holds the nested loop is a cost estimate (92,095 against 1,261,350), not a guarantee. `SET enable_nestloop = off` on the same rail query gives a Hash Right Join that scans all 100k threads and sequentially scans 200k messages: **531ms, a 23x regression, and no longer bounded by the page size.** The trigger now says to confirm the join is still a nested loop above the sort, not merely to re-time the filters.
>
> **HIGH 1 -- the emitMailEvent header argued against a decision this branch shipped.** Round 2 added `events_mail_thread_id_idx` but never revisited the "THE INDEX THIS DELIBERATELY DOES NOT SHIP" block, whose two halves were both false by then (the scan claim, and a ~100k-events re-measure trigger the index had already answered). Rewritten as "the index 0008 ships for exactly this query", obsolete trigger dropped, and the two blocks now name each other so neither can be updated alone.
>
> **HIGH 2 -- DEAL/PROJECT EMISSION WAS A GREEN MUTATION.** Setting `dealId: null, projectId: null` in emitMailEvent's insert left the FULL suite passing. Nothing anywhere ingested a message on a deal- or project-linked thread: this file's fixtures all reach their links through the AUTO-LINKER, which writes only contact and company, and timeline.test.ts's matrix hand-writes its rows and never calls emitMailEvent at all. So the headline use case -- and the ONLY link kind 4.2 treats as widening, i.e. exactly the entries another user is meant to see -- could have stopped reaching deal and project timelines in silence. Closed at `mail-ingest.test.ts:1202` through the existing compose-seed plumbing (`IngestMessageInput.links`), asserting the emitted row's `deal_id`/`project_id` AND that `listEvents` returns the entry on both records. Re-running the mutation now fails exactly that test.
>
> **RULING -- THREE PUBLISHES WIDENED WITH `["events"]`, closed here because Task 4 created the coupling.** This task made mail writes change what a TIMELINE renders, which was never true before: hiding a thread removes its entries from that viewer's timelines and a deal link adds them to other viewers', because both predicates run at READ time. Without the key a cached timeline goes on offering a click through to a thread the viewer just filed away. **Never a leak** -- the server re-filters every request, so a stale client holds only what it was already entitled to see -- but a dead surface. Widened: `mail-threads.ts`'s `publishThreadHint` (hide/unhide and every link write), `mail-accounts.ts`'s visibility-flip hint, and -- extending the same rationale to the third call site of the same act -- `mail-move.ts`'s `publishMoveHints`, which is the route the BULK "Hide in CRM" path takes and therefore the largest timeline change in the mail code. Over-invalidation is the accepted house trade (`updateMeeting` publishes `["events"]` deliberately while writing no event). Pinned at `mail.test.ts:1385` (link + hide, mutation-verified) and in the two existing exact-key-set assertions.
>
> **ATOMICITY, stated rather than left implicit.** Emission runs inside ingest's transaction, so a failure rolls the MESSAGE back: MailIngestError -> Phase 4's poison contract -> retry once, recordPoison, skip the UID. That is the intended trade -- a re-fetched message is a smaller harm than a stored message with its timeline entry silently missing -- and no realistic permanent failure exists to make it bite (every events FK is NO ACTION, the verb is a literal the CHECK accepts, the actor comes from the account row's own FK'd user).
>
> **TWO MORE THROTTLE CONSEQUENCES, in `emitMailEvent`'s header beside O3:**
> - **WITHIN A DAY THE FIRST MESSAGE WINS BOTH THE TIMESTAMP AND THE FK SNAPSHOT.** A thread whose only substantive reply lands at 23:00 renders at the 08:00 of that morning's throwaway note, under whatever links the thread carried then. The entry means "this conversation was active today", not "this is when the interesting mail arrived".
> - **A MAIL ACCOUNT ADDED AFTER THE UPGRADE IS A DE FACTO BACKFILL** -- and it is the ordinary case for the second and third user of a self-hosted CRM. Its first sync ingests the whole mailbox as `created: true`, so every auto-linked thread emits, all stamped NOW and ordered by IMAP UID rather than by when the conversations happened. 0008's NO BACKFILL note covers messages ALREADY STORED and does not cover this, because nothing is being synthesised -- these are ordinary first sightings. **TASK 6 MUST PUT THIS IN THE RELEASE NOTES beside the no-backfill line**, or a new user's first day looks like a timeline flood with no explanation.
>
> **TWO SILENT-PASS TRAPS CLOSED IN THE MATRIX.** `label()` must be injective over `Cell` and nothing enforces it -- add a fifth dimension, forget to extend `label()`, and sixteen pairs collide in `seen` and `expected` alike, agreeing because one iteration writes both, and half the matrix passes green; `expect(Object.keys(seen)).toHaveLength(MATRIX.length)` closes it. And the matrix queried at the default limit 50 while accumulating ~20 rows on one company, so a visible row that fell off the page read as EXCLUDED -- a PASS for every negative cell; it now passes `limit: 100`.
>
> **THE TIME-ZONE PIN, tightened three ways.** Both instants now derive from ONE `SELECT now()` against the DATABASE's clock rather than JS `Date` (the service compares against the database's `now()`, and for the few hundred milliseconds a year the two clocks straddle UTC midnight, correct code would fail an assertion built on the wrong one); the zoned URL is composed with `URL`/`searchParams` rather than string concatenation, since `TEST_DATABASE_URL` may already carry a query string; and a failed thread insert throws instead of falling back to `?? ""`, per `makeThread`'s own precedent.
>
> **O6 IS ALREADY PARTLY REACHABLE, and the header now says so.** `routes/events.ts` states outright that an unfiltered timeline is a valid request, so `GET /api/events?limit=100` returns a cross-record mail stream under `record` scope TODAY. It grants nothing the record Mail tabs do not -- identical gate, reviewer-verified -- so the condition O6 names is a UI change away, not a code change away.
>
> **FURTHER FOR TASK 5.**
> - **`mailSubject` can be the EMPTY STRING, not just null.** Ingest normalises `parsed.subject ?? ""` and `mail_threads.subject` is NOT NULL, so a subjectless message yields `""` on the wire. Reuse `NO_SUBJECT_LABEL` (`web/components/mail/mail-lib.ts:350`) rather than rendering a blank entry.
> - **Task 4 is the FIRST background writer of `["events"]`**, so invalidations of a scrolled, paginated timeline become routine for the first time. `rail/timeline.tsx`'s accumulate-on-cursor effect re-appends any loaded page whose contents change -- worth a look while Task 5 is in that file anyway.
>
> **PRE-EXISTING FLAKE, recorded not chased:** the reviewer hit `mail-sync.test.ts` "backs off exponentially to a 32-minute cap" failing once, passing twice in isolation on a clean tree. Unrelated to this task; do not chase it.

### Task 5: Web — the Meetings rail tab + timeline rendering

**Files:** `packages/web/src/components/rail/meetings.tsx` (new), `packages/web/src/components/rail/rail.tsx`, `packages/web/src/components/rail/timeline.tsx`, `packages/web/src/queries.ts`, `packages/web/src/components/mail/mail-lib.ts` or a new `rail/meetings-lib.ts` for pure logic

Read `packages/web/src/components/rail/notes.tsx` and `files.tsx` first — the new tab copies their structure (list + form + mutations, `useInvalidate*` patterns, testids).

- `rail.tsx`: fifth tab `Meetings`, testid `meetings-tab`, passing the same four record props the other tabs take.
- `meetings.tsx`: the list (title, when, attendee summary, follow-up-task count), a "Log a meeting" form (title, when — defaulting to now, duration, attendees, notes via `packages/web/src/components/mail/rich-text.tsx` — the TipTap editor the mail composer uses; note that notes.tsx is a plain Textarea, NOT TipTap, correcting this plan's first draft), a meeting view showing notes/attendees/tasks with an "Add task" affordance, archive + the house "show archived" control. Testids: `meeting-form`, `meeting-row-<id>`, `meeting-add-task`, `show-archived-meetings`.
- Attendee input: contact picker + user picker + free-text guest field, reusing the existing picker components (find them — do not build a new one).
- `timeline.tsx`: add the three verbs to `VERB_BADGE` (the map is exhaustively typed against `eventVerbSchema`, so the compiler names them) per spec Amendment 1's ruling: `met` = **M**, `mail_received` = **I**, `mail_sent` = **T**, and `dependency_removed` moves **M -> X**. Task 1 landed the spec's original letters and correctly flagged that two collided; commit 5271ea6 applied the amendment's letters AND fixed the map's head comments, so only `summarize()` is left — it has three cases to gain (a marker sits at its default arm; until then a `met`/`mail_received`/`mail_sent` row renders its raw verb). A `met` entry links to its meeting; a mail entry renders the derived `mailSubject` and links to the thread.
- Per the repo's pure-lib web-test convention (no testing-library in this project — see the 4.3 Task 3 DONE block), extract the label/summary logic into a lib module and unit-test THAT; render/interaction coverage is Task 6's e2e. ~10 tests.

### Task 6: e2e + release prep (v0.9.0)

**Files:** `e2e/meetings.spec.ts` (new), `e2e/mail.spec.ts` (if the privacy leg fits better beside the existing two-user fixtures), three `package.json`s, `manifest.toml`, server lockfile

e2e, runId + per-attempt (`${runId}x${testInfo.retry}`) fixtures per the 4.1/4.2/4.3 pattern:
- Log a meeting on a company with a contact attendee and a free-text guest → it appears on the company's Meetings tab AND on the contact's (the attendance widening, end to end) AND as a Timeline entry.
- Add a follow-up task from it → the task exists with the meeting's links inherited.
- A mail exchange on a linked record → a Timeline entry appears with the thread's subject.
- **The two-user privacy leg** (reuse the `ynh-user`-header second context from mail.spec.ts): B's timeline on the shared record shows NO entry for A's private thread — asserted against a loaded timeline (a sentinel entry B CAN see), per the 4.3 loaded-list-sentinel lesson.

Release prep: bump 0.9.0 (three package.jsons + manifest `0.9.0~ynh1`), regenerate the server lockfile, CI gate green on the pushed branch (report run id + tip sha — the run must cover the final tip), handoff artifacts in the session scratchpad: `release-notes-v0.9.0.md` (meetings, follow-up tasks, mail in the timeline, the no-backfill note so absent history is not read as a bug, the privacy rule stated plainly) and `release-sequence-v0.9.0.md` (the worktree-runnable sequence: `git push origin HEAD:main` → tag → Release workflow sha256 → manifest commit → Chris's sudo command → the spec's Rollout checklist). Do NOT merge, tag, or edit the manifest sha — the coordinator gates the release.

---

Sequencing: 1→2→3, then 4 (needs only Task 1's schema; may run once 1 lands), 5 after 2+4's contracts, 6 last. Each task: implement → adversarial spec review → quality review, the standing loop. The spec's Decisions table and the mail-privacy matrix are the completeness checklists.
