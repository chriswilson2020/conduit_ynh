# Conduit Phase 4.3 — Per-user Hide + backlog sweep Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Per-user Hide-in-CRM (hide rows per viewer, thread-global flag retired, Hidden view) plus the small-fixes sweep (detail cap, forward re-attach, archived-pipelines view, 1280px header, deepmerge-ts overrides) — released as v0.8.0.

**Architecture:** One table (`mail_thread_hides`, migration 0007 with backfill), one hide predicate composed with the 4.2 visibility family, one inverted-arm Hidden view on the existing list machinery, a payload-bound detail cap. Spec: `docs/superpowers/specs/2026-08-21-conduit-phase-4.3-backlog-design.md` — the authority.

**Tech Stack:** No new dependencies (the deepmerge-ts item is an overrides pin). All Phase 4/4.1/4.2 conventions BINDING (CONDUIT_REMOTE_DIR remote.sh; ASCII with String.fromCharCode fixtures; unshipped migrations editable in place with journal-timestamp preservation + psql convergence of conduit_test; the independent-EXISTS fold discipline; the three-unread-computations lesson; comment discipline: state only what was measured; targeted adds). Suite at start: 1547 unit + 36 integration + 50 e2e, green. Branch `worktree-phase-4.3-backlog` from the v0.7.0 manifest commit (58d0d63).

**Cross-phase sequencing note (load-bearing):** migration 0007 is built in TWO steps so every task compiles and stays green. Task 1 ships 0007 as CREATE TABLE + backfill only, with `mail_threads.archived_at` still present and every existing filter untouched. Task 2 appends the `DROP COLUMN` to the SAME 0007 file (unshipped → editable in place, journal timestamp preserved, conduit_test converged via psql) in the same commit that removes the column from schema.ts and swaps every reference to the hide predicate — the compiler then enforces the swap's completeness.

---

### Task 1: Schema 0007 (step one) + shared contracts

`mail_thread_hides`: `thread_id` FK, `user_id` FK, `hidden_at` timestamptz NOT NULL DEFAULT now(), PK (`thread_id`,`user_id`). Backfill in the same migration: INSERT one row per (archived thread × existing user) carrying `archived_at` as `hidden_at` — the upgrade-changes-nobody's-view decision. NO column drop yet (see sequencing note). Drill via `withPreMigrationDatabase("0007", ...)`: pre-0007 archived thread comes back hidden for every pre-existing user, unarchived for none. Shared contracts, all of them now with server placeholders where Task 2/3 fills in: thread list item + detail replace `archivedAt` with viewer-scoped `hiddenAt` (nullable ISO — comment that it is PER-VIEWER, sourced from the hide row, placeholder `null` until Task 2); `hidden` tri-state flag on threadListFilters (the archived-flag pattern); `totalMessages` + `truncated` on the detail schema + the `all` query param (placeholders: full count, false — faithful no-ops until Task 3). db:generate drift-free; conduit_test converged. ~8 tests.

> **DONE** (commits b403097 + the spec-review micro-round). As built: 0007 (`drizzle/0007_overjoyed_titania.sql`) is CREATE TABLE + the hand-written backfill INSERT ONLY — `mail_threads.archived_at` stays, every existing filter stays, Task 2 appends the DROP to this same file (unshipped, so editable in place with the journal timestamp preserved). FKs are plain NO ACTION, matching every FK in schema.ts (archive-not-delete; no delete path exists to cascade from) — "cascade" was considered and rejected as off-house-style. The drill (schema.test.ts:745) seeds two users + an archived + a live thread on a genuine 0006 database, threads via RAW SQL naming `archived_at` (so the seeding survives Task 2 removing the column from schema.ts), pins the hides table ABSENT pre-migrate, then asserts archived-hidden-for-both with `hidden_at` exactly `archived_at`, live-hidden-for-none, and `archived_at` STILL PRESENT. Suite 1554 (+7 on the 1547 baseline) + the micro-round's pins; typecheck clean; db:generate drift-free.
>
> **As-built amendment (supersedes "placeholder `null`" above, spec-review-endorsed):** the `hiddenAt` placeholder is NOT null — `toThread` maps `row.archivedAt?.toISOString() ?? null` (mail-threads.ts:57). A literal null would have CHANGED behaviour this task must not change (the conversation view's Unhide state and three archive/unarchive route tests read the field), contradicting the sequencing note's "every existing filter stays". The stand-in is exact, not approximate: pre-4.3 every viewer shares the one thread-global hide state, and 0007's backfill writes precisely `archived_at` into every user's hide row, so the thread-global value and every viewer's would-be per-viewer value agree by construction until Task 2 swaps the source.
>
> **Rename ruling (spec-review-endorsed after a full consumer sweep — web, routes, api tests, integration, e2e):** `hidden` REPLACES `archived` on threadListFiltersSchema and the wire (the spec's `?hidden=true` spelling); same for `hiddenAt` replacing `archivedAt` on the payload shapes. Client and server ship as one unit, so no alias is kept. Accepted consequence, documented and pinned: zod strips unknown keys, so a stale `?archived=true` yields the DEFAULT list silently (behaviour pinned in routes/mail.test.ts's filter test; absence-from-shape pinned in shared/index.test.ts for both the filter flag and the payload field). Default view stayed byte-identical.
>
> **HANDOFFS for Task 2 (every placeholder site):**
> - `drizzle/0007_overjoyed_titania.sql` — append the `DROP COLUMN archived_at`; conduit_test then converges via psql (edited-in-place migration), per the standing convention.
> - mail-threads.ts:57 (toThread's `hiddenAt` — source from the viewer's hide row), :559 (listThreads' hide arm — swap the archived_at test for the hide-row EXISTS, composed with, never replacing, the 4.2 visibility terms; fold discipline applies), :856 (detail-cap pair — Task 3's, not Task 2's).
> - shared/index.ts:775 (`hiddenAt` comment), :987 (`hidden` flag comment), :922 (detail-cap pair — Task 3) — rewrite each placeholder note as the real source lands.
> - routes/mail.ts:87 (`hidden` wire flag), :108 + :382 (`all` param, parsed-and-ignored — Task 3 threads it into getThreadDetail).
> - schema.test.ts:745 drill — Task 2 flips the final `archived_at` STILL-PRESENT assertion to ABSENT and extends the post-migrate asserts; the raw-SQL thread seeding already compiles without the schema.ts column, so only the assertion arm changes.
> - Open EXPLAIN question (deliberately unanswered here): the PK serves thread→hides; whether the per-user list predicate wants a `(user_id, thread_id)` index is measured in Task 2 and, only if warranted, added to 0007 while it is still unshipped.
> - web sites already on the new names (mechanical, behaviour identical): queries.ts:1485 + its qs mapping, inbox.tsx:116, conversation.tsx:248 (local renamed; testids untouched — they are addresses, not labels).
>
> **Quality-review handoff additions (coordinator-recorded):**
> - **The snapshot edit (M1, load-bearing):** appending the DROP to 0007 is NOT drift-free by itself — `meta/0007_snapshot.json` still lists `archived_at` under `public.mail_threads.columns`, and drizzle-kit diffs schema.ts against the newest snapshot. Task 2 must hand-delete that column entry from the snapshot (keep `id`/`prevId` intact), then re-run db:generate and confirm "No schema changes". This is the first in-place edit of a drizzle-TRACKED object (0005's precedent was hand-written indexes with no schema.ts representation, so its snapshot never needed touching).
> - **The link-guard ruling (M2, coordinator):** the archived-thread 409 on link changes RETIRES with the shared state — a per-person hide must never gate a shared CRM mutation, in either direction (A's hide blocks neither B's link nor A's own; linking from the Hidden view or detail is a deliberate act, and per 4.2 a deal link SHARES the thread while it stays hidden in A's own inbox — coherent, intended). Delete the guard at :977/:991/:1002 including writeLink's `isNull(archivedAt)` WHERE arm (its TOCTOU purpose dies with the state it guarded; the UPDATE keeps the id predicate alone). REWRITE — don't delete — the 409 test (routes/mail.test.ts:1218) to pin the new truth: a hidden thread links fine, for the hider and for another user. Recorded as spec Amendment 1.
> - **Convergence scope (wider than conduit_test):** the migrator compares only timestamps, never hashes, so an edited 0007 silently never re-applies to ANY database already at 0007 — psql-converge every such DB on the server (conduit_test, Playwright's reused DB, any dev/smoke install).
> - **The "three unread computations" is TWO swap sites here:** only unreadThreadCount (:1154) and unreadCountsByFolder (:1210) carry their own `isNull(archivedAt)` term; the list row's unread aggregate and `?unread=true` inherit listThreads' WHERE. (search.ts:74 is the third standalone site, plus writeLink/setArchived per the ruling above.)
> - **toThread shape:** writeLink's UPDATE..RETURNING cannot join the hide table; fetch the viewer's `hidden_at` once in mustGetThread and pass it as toThread's explicit second argument — setThreadLink/clearThreadLink already call mustGetThread first, so one internal signature change covers all 7 call sites, no route change.
> - **setArchived successor shape (review-verified):** hide = `INSERT .. ON CONFLICT (thread_id,user_id) DO NOTHING RETURNING *` (empty returning = already-hidden, mirroring today's guarded-UPDATE no-op; re-read arm becomes a SELECT of the hide row); unhide = `DELETE .. RETURNING *`. No transaction, no new locking (mail_threads is never FOR UPDATE'd; the FK takes only FOR KEY SHARE, compatible with ingest's non-key updates).
> - **EXPLAIN hint for the index question:** the default arm's NOT EXISTS is an exact composite-PK probe — the case worth measuring is whether the HIDDEN VIEW should drive from mail_thread_hides (semi-join hides→threads) rather than probe per candidate thread; only that would earn `(user_id, thread_id)`.

### Task 2: The hide predicate + every read path + service surgery

Append `DROP COLUMN archived_at` to 0007; remove from schema.ts; the compiler lists every swap site. `hiddenByViewer(userId)` helper in mail-threads.ts composed with (never replacing) the 4.2 family — default surfaces get `AND NOT hidden`, the Hidden view (`?hidden=true`) gets `AND hidden` on the same list machinery (folder/account filters compose unchanged; fold discipline applies — the hide arm is a thread-level EXISTS, visibility stays message-granular). Swap sites per the compiler + the spec: list, all three unread computations, record tabs, search (search.ts's `isNull(mailThreads.archivedAt)` becomes the not-hidden arm — it needs the userId it already has), mail-move's hide path. Detail stays accessible for hidden threads (mustGetThread unchanged); `hiddenAt` surfaced on list + detail from the viewer's row. `setArchived`/`archiveThread`/`hideThreads` become per-actor hide-row insert/delete: idempotent both ways, bulk vocabulary unchanged, publishThreadHint unchanged (per-user results at refetch). The hide matrix tested per the spec's Testing section — hider/other × every surface, composed with visibility (hidden-but-invisible stays invisible). ~28 tests. The phase's core task.

> **DONE** (commits b15bfcb + the spec-review round). As built:
>
> - **Migration second half:** 0007 gained the `DROP COLUMN archived_at` after the backfill (journal timestamp untouched); `meta/0007_snapshot.json` hand-edited to delete the column entry (id/prevId intact — the first in-place edit of a drizzle-TRACKED object, per the M1 handoff) and db:generate confirms "No schema changes". The drill's final assertion flipped (archived_at ABSENT) and its post-migrate asserts now run the REAL listThreads against the upgraded scratch database: a shared account + pre-migration messages make both users' default inboxes and Hidden views prove the backfilled hide rows drive the exclusion, each viewer's `hiddenAt` carrying the pre-upgrade `archived_at`.
> - **Predicate shape:** `hiddenByViewer(userId)` / `notHiddenByViewer(userId)` exported from mail-threads.ts — a thread-level EXISTS on mail_thread_hides correlated to the outer un-aliased mail_threads (visibleThreads' precondition), composed as its own top-level term (documented why the fold discipline does not apply: no per-message hide fact to share a row with).
> - **Swap sites, all nine:** listThreads' hide arm (`opts.hidden ? hiddenByViewer : notHiddenByViewer`, both views on one machinery); unreadThreadCount + unreadCountsByFolder (the two standalone unread sites; list flag/filter inherit); search.ts:74 → `notHiddenByViewer(userId)`; writeLink's `isNull(archivedAt)` arm DELETED (Amendment 1 — the UPDATE keeps the id predicate alone, undefined arm now a defensive NotFoundError); setThreadLink/clearThreadLink guards deleted; setArchived → setHidden; mail-move's hide path via the renamed hideThread.
> - **Service surgery:** `toThread(row, hiddenAt)` takes the viewer's hide state as an explicit second argument; mustGetThread returns `{thread, hiddenAt}` via a LEFT JOIN on the viewer's own hide row (visibility-only gate unchanged — hidden detail stays open); listThreads' page query carries `hiddenAt` per row through the same LEFT JOIN (null by construction on the default view, the filing moment on the Hidden view — no N+1). Hide = `INSERT .. ON CONFLICT (thread_id, user_id) DO NOTHING RETURNING *` with the no-op arm RE-READING the standing hide row (a repeated hide reports the ORIGINAL filing moment — pinned); unhide = `DELETE .. RETURNING *`; no transaction, the shared thread row never touched (no updated_at bump — pinned by the byte-identical-world test). `archiveThread`/`unarchiveThread` renamed `hideThread`/`unhideThread`; the wire keeps its `/archive`,`/unarchive` paths (an address, not a label). Single-thread hint fires only on a real write (no-ops publish nothing — pinned); the bulk path keeps its one-batch-frame trade.
> - **EXPLAIN + index decision (recorded in 0007 and beside listThreads):** 0005/0006 methodology, 20,000 threads / 80,000 messages, two private accounts, 1,250 hides per user with the viewer's skewed old (the Hidden view's worst case). Default list 0.70ms / 773 buffers (NOT EXISTS = composite-PK index-only probe, Heap Fetches 0); Hidden view worst case 11.8ms / 21,317 buffers (LIMIT-ordered thread scan probing per thread), UNCHANGED by the candidate `(user_id, thread_id)` index (12.7ms / 21,317 — same plan, different probe target) and barely moved by a forced drive-from-hides CTE (9.7ms / 4,046 — the per-thread visibility EXISTS dominates either way). **No second index ships.** Badge 9.3ms / 364, per-folder 10.1ms / 370, unseen partial index still Heap Fetches 0.
> - **Convergence audit (corrected record, verified as postgres):** `conduit_test` was the only database at the pre-edit 0007 — converged via psql `DROP COLUMN`; its recorded migration hash diverges from the edited file, which is inert-and-expected (the migrator compares timestamps, never hashes). Playwright reuses conduit_test. `conduit_dev` sits at 0003. **Prod `conduit` is at 0006** (drizzle.__drizzle_migrations: 7 rows, 0006's hash matching the repo; archived_at present, no hides table; 2 users, 28 threads, 1 archived) — a chris-privilege information_schema probe initially misread it as drizzle-less; the superuser check corrected that. At the v0.8.0 upgrade prod runs the edited 0007 WHOLE — create + backfill + drop in one transaction — which IS the migration promise and belongs in the release notes.
> - **Tests:** +12 on the 1554 baseline → suite 1566 + 36 skipped green on the server; typecheck clean (five projects). New per-user-hide describe (hider/other × default list, Hidden view with hiddenAt correctness, detail, badge, per-folder, inherited unread filter, record tabs + hidden×record composition, Hidden×folder/account/unread, and BOTH never-leaks halves — invisible-thread no-hint-of-existence AND the visible-thread byte-identical world comparison); the SSE no-publish pin for idempotent no-ops (mail-move.test.ts); Amendment 1's rewritten pin (own link + other's link + deal-link-shares-while-hidden); per-actor rewrites of every archived-thread fixture (routes list/filters/mark-read/idempotency-with-preserved-timestamp/bulk, search route + service, mail-move's hide tests, the invisible-mutation sweep). The visibility/per-user describes share hoisted `setVisibility`/`listIds` helpers (dedup per quality review).
>
> **HANDOFFS for Task 3:** the detail-cap placeholders are the only Task-1 stand-ins left — mail-threads.ts:949 (totalMessages/truncated pair), shared/index.ts:916 (schema comment), routes/mail.ts:104 (`all` parsed-and-ignored; thread it into getThreadDetail). The web can rely on `hiddenAt` being genuinely per-viewer now: null by construction on every default list row, the viewer's own filing moment on Hidden-view rows and on a hidden thread's detail (conversation.tsx's Hide/Unhide state comment already rewritten to that truth).
>
> **HANDOFFS for Task 4:** the e2e two-user hide journey runs against real per-actor semantics (no fixture shims needed). Release notes carry: the migration promise ("upgrade changes nobody's view"), prod's one-transaction 0007 path (create + backfill + drop — verified pending at 0006 with 1 archived thread × 2 users to backfill), and the no-index EXPLAIN decision recorded in 0007. No further database convergence exists anywhere: every remaining database meets the edited 0007 whole.
>
> **Quality-review round (coordinator-recorded, closes Task 2):**
> - **Spec Amendment 2 (ruled):** ingest into a hidden thread STAYS hidden — the thread bump touches no hide row, so the filing survives new mail (resurfacing is snooze territory, deferred). Pinned two-sided in mail-ingest.test.ts (hider's list + badge unchanged, Hidden view carries the grown conversation; the other viewer's surfaces gain it normally); the deliberate non-write documented at ingest's thread-bump comment (the spot a resurface change would touch) and cross-referenced from setHidden's JSDoc. Reply-on-a-hidden-thread pinned service-level beside the reply-gate tests (mustGetThread's gate is visibility-only; the filing stands after the send).
> - **SSE known cost (accepted for v0.8.0):** hide/unhide is the first provably actor-only mutation, but sse.ts's global fan-out still delivers the invalidation frame to every client — a 200-thread bulk hide makes users whose data is byte-identical refetch three key families. Accepted; actor-scoped SSE hints logged as deferred backlog. The visible-thread world-comparison test's comment now scopes its claim to HTTP and names this exception.
> - **Task 3 handoff (web renames, before Task 4's e2e reaches for them):** rename `useArchiveThread`/`useUnarchiveThread` (queries.ts:1573/:1582) → `useHideThread`/`useUnhideThread` and the `archive-thread`/`unarchive-thread` testids (conversation.tsx:312/:321) → `hide-thread`/`unhide-thread` WHILE adding the Hidden-view UI; `conversation-archive` (the real IMAP move, conversation.tsx:290) keeps its name — two actions spelled "archive" on one screen is the standing hazard the rename resolves.
> - **Drill coupling acknowledged:** the drill's listThreads call couples it to service signatures and visibility rules — accepted for the through-the-app's-eyes realism, recorded in the drill's own comment; the raw-seeded threads now carry message_count matching their messages.
> - **Comment precision (all landed):** schema.ts records the ANSWERED index question and the corrected no-action-FK reason (nothing is ever deleted); `hiddenByViewer` is module-private (search imports only `notHiddenByViewer`, which now carries the exported-for note); setHidden's re-read comment names the second null path (ON CONFLICT DO NOTHING does not wait out an uncommitted winner — one-beat stale answer, self-healing via the winner's hint); listThreads' join comment states the one-statement/one-snapshot reason "null by construction" holds plus the uniform-shape justification (~2 buffers/row measured); 0007 carries the re-measure trigger (badge/per-folder hash anti-join the whole hides table — linear; re-EXPLAIN at order 25k+ hides).

### Task 3: Detail cap + forward re-attach + web sweep items

API: `getThreadDetail` returns the newest 50 VISIBLE messages (cap applies after the visibility filter — the payload bound is on what renders), `totalMessages` (visible total) + `truncated`; `?all=true` uncapped. Mark-read and reply-chain semantics unchanged (they already read the full/newest-visible sets server-side — assert in tests, boundary at exactly 50). Forward re-attach in mail-send: outgoing forward carries the original message's stored attachments (existing compose size caps enforced; test the cap refusal). Web: "Show earlier messages (N more)" control; Hidden view UI (inbox filter + Unhide button, testids); `hiddenAt`-aware conversation Hide/Unhide button state; the 1280px conversation-header fix. ~16 tests.

> **DONE** (commits d6ab881 + 7aa62d4 + the spec-review round + the quality round). As built:
>
> - **Detail cap shape (as amended by the quality round's window collapse):** `getThreadDetail(db, userId, id, apiBase, {all})`, `THREAD_DETAIL_MESSAGE_CAP = 50`. ONE statement: the page query carries `count(*) OVER ()` and `bool_or(owned) OVER ()` as window aggregates (evaluated after WHERE, before LIMIT), always DESC `(sent_at, id)` with LIMIT 50 unless `all`, reversed in JS — the truncated page is the newest 50 IN THE SAME TOTAL ORDER the conversation renders, i.e. exactly the ascending tail, and `truncated` is literally rows-shorter-than-total. One snapshot ⇒ the shared schema's invariant (uncapped ⇒ totalMessages = messages.length) holds BY CONSTRUCTION — a concurrent ingest cannot wedge a contradiction between the page and its totals, which the earlier two-query shape permitted. Detail vs reply-chain tie-break asymmetry (`(sent_at, id)` vs `(sent_at, created_at, id)`) is pre-existing and cross-referenced at the detail's ORDER BY.
> - **ownedByViewer aggregate move — a behaviour FIX, not a refactor:** pre-cap it was read off the returned rows; a capped page can drop the viewer's only owned message (older than the newest 50), which would have flipped the conversation's Archive/Trash buttons off for a thread the viewer owns mail on. Pinned: shared-account thread, 52 unowned-visible + 1 old owned, truncated page omits the owned row, `ownedByViewer` still true.
> - **Cap × visibility pinned two-sided** (52 own + 3 other-private: chris sees totalMessages 52/truncated/newest-50-of-his; dana sees her 3 untruncated — per-viewer arithmetic, invisible messages neither count nor fill slots). Attachments of non-returned messages: metadata absent from the payload, bytes still fetchable by id (record-visible; getAttachmentBlob runs its own per-message check) — pinned by downloading a truncated-away attachment.
> - **Mark-read (spec-review D2 ruling, behaviour fixed; quality round Issue 1 closed the cascade):** the conversation's mark-read effect fired off a client-side `some(!seen)` over the RENDERED page — falsified by the sync engine (flag reconcile un-seeing old rows; initial sync ingesting old unread), leaving a >50 thread whose only unseen rows sit below the page stuck in the badge. The effect now fires UNCONDITIONALLY once per opened thread (markedRef is the once-guard); idempotence is the server's job and already held (markThreadRead writes only unseen rows, publishes no hint on a no-op). The unconditional fire made the CLIENT's cascade visible — useMarkThreadRead invalidated 4 key families per open of an already-read thread — so the read route's response is now `{thread, changed}` (shared `markThreadReadResponseSchema`) and the hook gates its invalidations on `changed`: a no-op open costs one POST and nothing else (no hint server-side, no refetches client-side; SSE stays the always-path for real changes). Pinned: shared schema (changed required — an absent flag would make the client guess), route-level changed true/false on first/second read, changed TRUE in the discriminating truncation shape (newest 50 seen + one unseen below the page — the page shows zero unseen yet the write changed something, the exact case the client cannot derive), and the below-page row + badge clearing. The gate itself is a one-line `if` in the hook — not extractable into a testable lib function without inventing one; its click-through is Task 4 e2e's like the rest of the web interaction layer.
> - **Reply chain at the boundary (7aa62d4, comment corrected per D3):** a 51-message thread's reply carries In-Reply-To naming the true newest. A modest pin, stated honestly in its comment: the capped page's ascending tail contains the newest by construction, so this pins the behaviour, not the implementation route.
> - **Forward re-attach:** `sendMailInputSchema.forwardAttachmentIds` — mail_attachments ids, `.max(50)` (mirrors ingest's MAX_ATTACHMENTS), deduplicated server-side (doubled id attaches once — pinned), and honestly documented as accepting ANY record-readable attachment id (the right is getAttachmentBlob's download rule; "forward" names the flow, not a narrower contract). `loadForwardAttachments` reuses getAttachmentBlob per id — authz + storage reuse bought a deliberate, documented N+1, bounded by the 50-id wire cap, each probe a PK join. Size cap `MAX_FORWARD_ATTACHMENT_BYTES` (50MB, per attachment, no total cap — exactly the compose semantics) checked at send time against stored size_bytes, refusing the WHOLE send via `AttachmentTooLargeError` → **413 `too_large`** (the upload route's own status+code, new mapDomainError branch). BELT AND BRACES today, per the D4 ruling: ingest's 25MB raw cap bounds any stored attachment to ~19.6MB, so the refusal is unreachable through current write paths (both cap tests state their over-cap row by UPDATE). **Three-literal coupling, named at all three sites:** routes/index.ts multipart fileSize ↔ mail-send.ts MAX_FORWARD_ATTACHMENT_BYTES ↔ mail-ingest.ts MAX_ATTACHMENT_BYTES — one ceiling, change together. Ingested forward keeps its own attachment rows (pinned); invisible original 404s before anything sends (pinned); route-level 201+413 wire pin.
> - **Renames (Task 2 handoff + D8):** `useHideThread`/`useUnhideThread` (wire keeps `/archive`,`/unarchive` — addresses); testids `hide-thread`/`unhide-thread` (conversation.tsx; `conversation-archive`, the real IMAP move, keeps its name); the two existing e2e assertions moved along. **`filter-archived` → `filter-hidden`** (D8 ruling: nothing referenced it yet — verified — and the window closed with Task 4's e2e; same two-actions-spelled-archive rationale). Hide/Unhide button state reads `thread.hiddenAt` directly; Hidden-view rows carry a `hidden-chip` ("Hidden 2h ago", mail-lib's hiddenChipLabel — null-by-construction hiddenAt means it appears exactly on Hidden-view rows); bulk copy audited for per-user semantics (no user-facing string implied hide-for-everyone; one stale bulk-bar CODE comment rewritten).
> - **Show-earlier:** capped/uncapped useMailThread pair under the one `["mail-thread", id]` prefix (every existing invalidation reaches both); capped page stays rendered while `?all=true` loads; testid `show-earlier` with pending/error/explicit-refetch retry; label from mail-lib's showEarlierLabel (unit-tested, truncated flag stays the gate — no client re-derivation).
> - **1280px cause/fix (reproduced before fixing,** static harness of the exact DOM against the built CSS at 1280×800): the third grid column leaves the conversation pane ~416px; the header's four actions (~426px single row) sat in a `shrink-0` group inside a no-wrap row, OVERFLOWING and clipping at the pane edge while the `min-w-0` subject crushed to a one-character-per-line strip. Fix = settings-mail's house idiom: `flex-wrap` row + shrinkable self-wrapping `ml-auto justify-end` action group; verified fixed at 1280 and one-line-unchanged at 1728.
> - **Quality-round cheap items (all landed):** the forward cap documented as belt-and-braces in ingest's own language (unreachable through current write paths — ~19.6MB real ceiling; both cap tests state their over-cap row by UPDATE); the three 50MB literals name each other at all three sites; loadForwardAttachments' authz-reuse N+1 documented with its bounds; the max(50)-BEFORE-dedupe order stated on the schema and pinned (51 ids with a duplicate 400 even though deduping would fit); the per-viewer cap test's invisible messages moved to the NEWEST end so a cap-before-filter regression fails the page-id assertion, not just the count; a missing blob at compose documented (storage corruption only, bare 500, nothing sent or stored — accepted, pre-existing for uploads); the header row keeps ml-auto as its ONE alignment mechanism (dead justify-between dropped); the capped detail query disables while the uncapped one is live (showAll resets per thread via the key remount — inbox is the only mount site); the composer's "needs a linked record" note moved below the chips so it no longer contradicts working forwarded chips.
> - **Tests/suite:** 1566 → **1585** (+19) + 36 skipped, green on the server; typecheck clean (five projects); db:generate drift-free (no schema change).
>
> **HANDOFFS for Task 4:**
> - e2e testids: `show-earlier`, `hide-thread`/`unhide-thread`, `hidden-chip`, `composer-forward-attachment-<id>`, **`filter-hidden`** (renamed this task — do not reach for `filter-archived`); `conversation-archive` unchanged.
> - **Deferred web render/interaction coverage is Task 4 e2e's, explicitly:** repo web tests are pure-lib (verified — no DOM/testing-library anywhere), so Show-earlier click-through, the Hidden chip's on-screen flow, the renamed buttons, the hiddenAt-driven button state and the mark-read changed-gate are asserted only at lib/route level here and land as e2e journeys.
> - **The 1280px fix needs its e2e viewport assertion** — the only guard the fix will ever have: at a 1280-wide viewport the conversation header must not overflow its pane (all four action buttons visible and clickable). Without it the next layout change regresses silently.
> - The two e2e edits in d6ab881 (archive-thread → hide-thread at mail.spec.ts:953/:1011) are typecheck-verified only until Task 4's CI gate actually runs the Playwright suite.
> - **Show-earlier fixture answer: affordable.** e2e seeds via raw IMAP APPENDs (mail.spec.ts's seedMailbox); 51 generated raws sharing subject + References chain in one connection is cheap and one sync pass ingests them. Fall back to route-level only if the 51-ingest sync wait proves flaky.
> - The three-literal 50MB coupling above, should the deps/e2e work touch upload or ingest bounds.
>
> **Backlog notes (coordinator-accepted, NOT this phase):** bulk Unhide in the Hidden view's bulk bar (Hide is live-but-meaningless there today — idempotent, harmless); forwardBody still embeds this CRM's /api inline-image URLs in the quoted body an external recipient cannot fetch, while re-attach now duplicates the same image as a plain attachment (pre-existing, made visible by re-attach); extracting the Show-earlier state machine into a testable `useThreadDetail(threadId)` hook (currently untestable at any level below e2e); folding the three 50MB literals into one shared constant (preference only — the comment web is greppable today).

### Task 4: Archived pipelines + deps + e2e + release prep

Company detail: read-only archived-pipelines view (API list filter param if missing + web "show archived" control, matching the deals-side archive philosophy; authz unchanged). deepmerge-ts overrides entry in the root package.json; lockfile regenerated ON THE SERVER; full suite green proves the pin changes nothing. e2e: two-user hide journey (A hides — gone from A's inbox/search, present in A's Hidden view, B's view untouched; A unhides — restored); Show-earlier route-level unless a >50-message fixture is cheap; archived-pipelines visible. RunId + per-attempt retry-scoped fixtures. Release prep 0.8.0: version bumps (3 package.jsons + manifest), server lockfile, CI gate on the pushed branch, handoff artifacts (release notes covering the migration promise "upgrade changes nobody's view", sequence sheet citing the merge candidate's run, live checklist per the spec's Rollout). Coordinator gates the merge.

> **DONE** (commits 8868abb + f1a1982 + 2499e1e + ab31ccb + the docs commit). As built:
>
> - **Archived pipelines (A):** the API filter already existed — `listPipelines`' archived arm has been service-tested since P2 and the list route's tri-state `archived` flag ("true"/"false"/absent) shipped with the route — so the "if missing" branch never opened. What was actually missing, found by reading before writing: route-level proof (the standing routes test CLAIMED "and archived" in its title for two phases without ever sending the flag) and any UI reaching the filter with a companyId. Web: company-detail's Pipelines section gains the house Archived checkbox (testid `show-archived-pipelines`), swapping `usePipelines({ companyId, archived })` between live and archived exactly as the pipelines index and entity-table do; archived rows wear an `pipeline-archived-chip` (archivedAt null-by-construction on the default view — the hiddenAt-chip idiom); empty label says which view is empty. Read-only needed NO new gating and the component comment says why: the section renders pipelines as plain links and the board they open already gates every mutating affordance on an archived pipeline (board.tsx readOnly, Phase 3.1); the section's one mutation, New pipeline, creates a fresh live pipeline either way. **The "~6 unit tests" became +1** (routes 86 → 87): the wire pin got its own case (all three tri-state spellings against one company's live+archived pair, archivedAt non-null on the archived rows), the mistitled test became true by subtraction, and no web lib was extracted — the control is a query-param swap plus a string ternary, beneath the pure-lib pattern's threshold, and inventing a lib to hit a test count would have been the wrong trade.
> - **deepmerge-ts (B), the advisory:** GHSA-ggr8-5vv4-36mx (stack exhaustion on recursive object graphs), affected <8.0.0, patched 8.0.0 — found via npm audit on the server as planned; we pin `^8.0.0`, lockfile records 8.0.2. Reached through mailparser → html-to-text@10.0.0 (mailparser pins html-to-text EXACTLY, and 10.0.1 — which wants ^8.0.1 on its own — is out of mailparser's reach, hence the override rather than an ordinary update). **npm 11.17.0 made the pin earn its place, and the record matters for the next override:** npm does not reconcile a root override into an already-satisfied WORKSPACE lockfile — plain install, `--force`, `update <pkg>` and `dedupe` all kept 7.1.6 (one sequence left the tree with the package MISSING entirely) — and `npm ls` misreports the overridden edge as "invalid" under workspaces even when the override WAS applied. Both halves scratch-reproduced on the server: a non-workspace fresh resolve honors the override and reports clean; a workspace fresh resolve honors it at install time and still mislabels it. The committed lockfile entry is therefore npm's OWN fresh server-side resolution of 8.0.2 transplanted verbatim (integrity and engines included); `npm ci` then installs it (CI's install path — verified on the server), mailparser loads against it, npm audit no longer lists the advisory, and the full suite green is the pin-changes-nothing proof. Lockfile diff verified: the pin + the three 0.8.0 version bumps, nothing else.
> - **The make-release discovery (B, unplanned but load-bearing):** make-release.sh generates the tarball's runtime package.json from api's dependencies alone and resolves a FRESH stage lockfile — so the root override would have silently never reached the tarball, and production's `npm ci --omit=dev` would have kept installing 7.1.6. make-release.sh now carries the root `overrides` into the generated package.json (the stage is a non-workspace context, where the override provably applies). Dry-run on the server: the staged package-lock.json records deepmerge-ts 8.0.2. The esbuild/drizzle-kit audit item remains open, deliberately: its only fix is a drizzle-kit downgrade npm itself labels a breaking change, and it was never this task's scope.
> - **e2e (C), NO fallbacks taken:** the 51-APPEND fixture was affordable exactly as Task 3 predicted — seeded in beforeAll BEFORE the account exists, so the account's FIRST sync pass ingests it and no mid-journey IDLE wait is needed; the sync test now also waits for the full 4.3 ingest (the long row's snippet reading "Message 51" is the whole-chain signal), preserving the unread-badge delta test's no-concurrent-ingest precondition. Forward re-attach landed as the FULL happy path, not the chip-only minimum: a multipart fixture's stored attachment shows as the `composer-forward-attachment-<id>` chip after openForward, the forward is addressed and SENT, and Mailpit's message DETAIL lists the attachment by name — re-attach proven on the wire. The 1280px assertion is horizontal CONTAINMENT of all four header actions in the conversation pane's bounding box at an explicitly-set 1280-wide viewport (containment, not visibility: clipping satisfies visibility). The two-user hide journey runs on real per-actor semantics: A hides via the conversation's `hide-thread` (button flips to `unhide-thread` on the open pane — hide is a filing act, not a lock, on screen), the thread leaves A's WHOLE loaded inbox and A's search while a same-marker sentinel thread keeps answering (the sentinel is what makes the search negative a statement about a loaded list — "No results" also renders mid-flight), appears in A's Hidden view behind `filter-hidden` wearing `hidden-chip`, B's inbox holds it untouched throughout, and unhide restores it; the mailbox is re-shared for the journey and re-privatized after (the 4.2 hygiene invariant). Archived pipelines joined crm.spec.ts's journey (create on the company page, archive on the board, find it behind the control, open into the read-only board). Retry safety: every 4.3 fixture is per-attempt (`${runId}x${testInfo.retry}`, Message-IDs included) because the cap test asserts a COUNT and the hide journey asserts whole-list negatives; fixtures are dated hours old so earlier journeys' page-one geography is untouched, and a bounded `loadAllThreadsOn` makes every 4.3 row assertion about the whole accumulated list. **e2e 50 → 57** (mail +6, crm +1); d6ab881's migrated assertions got their first real run. Honest limits: bulk-hide's click-through stays lib/route-covered (the conversation pair is the journey's surface); SSE delivery is never asserted as an SSE fact (house pattern — pollWithReload is the belt); B's search view of the hidden thread is not asserted (B-side scope is 4.2's matrix).
> - **Release prep (D):** three package.jsons + manifest to 0.8.0/`0.8.0~ynh1`; manifest source url/sha UNTOUCHED at v0.7.0 (the coordinator's post-tag commit moves them); one server lockfile regeneration covered the pin and the bumps. **CI confirming run: 32519601017 (commit 2499e1e)** — e2e job green in 1m52s, **57 passed (32.4s), zero retries burned**, the 4.3 journeys and the migrated assertions green on their first CI outing; test job green (typecheck + vitest incl. the 36 MAIL_IT integration). The final docs+test-split tip's own run is recorded in the task report and the release-sequence artifact (the sequence sheet cites the run covering the merge candidate's exact sha). Handoff artifacts in the session scratchpad: `release-notes-v0.8.0.md` (user-terms Hide story with the migration promise, detail cap, forward re-attach, archived pipelines, 1280px, the pin; small print: Amendment 2's ingest-stays-hidden, Amendment 1's link-guard retirement, prod's one-transaction 0007) and `release-sequence-v0.8.0.md` (ff-merge → tag → release sha → manifest commit → Chris's sudo upgrade command → the spec-Rollout live checklist, plus the prod-at-0006 migration note and a deepmerge-ts-reached-prod check).
> - **Marker sweep (E):** no forward-looking phase-4.3 marker survives in sources — verified by sweep; every remaining "Task 4" comment in packages/ is Phase 4.1's Task 4 (folders), historical truth, and inbox.tsx's rename-window comment now describes a closed window. Suite: **1586 unit + 36 integration** (baseline 1585 + the split-out wire pin) + 57 e2e; typecheck clean across the five projects; db:generate untouched (no schema change this task).

---

Sequencing: 1→2 strict (the sequencing note); 3 after 2 (same files); 4 last (its e2e needs 2+3). Each task: implement → spec review → quality review, the standing loop.
