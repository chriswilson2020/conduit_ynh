# Conduit v1.2.0 — Phase 7.5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clear the findings list Phases 6 and 7 wrote down and deliberately did not fix.

**Architecture:** Four independent items. Nothing here adds a feature; three are corrections to interaction and one is a test-infrastructure defect. No migration expected — if a task believes it needs one, that is a finding to report rather than implement.

**Spec:** `docs/superpowers/specs/2026-08-29-conduit-phase-7.5-accessibility.md` — the authority.

**Baseline:** `origin/main`, **2443 unit / 0 skipped, 119 e2e**. Branch `worktree-v1.2.0-a11y`.

---

## Conventions every task must follow

Each of these cost a CI round or a review finding in Phase 7 or v1.1.0.

- **Migrations are generated, never hand-written, and NEVER `drizzle-kit push`.** Revising an unshipped one means regenerating it, restoring the journal's `when`/`tag`, verifying the journal diff is empty, and **dropping and recreating `conduit_test`** — the migrator skips by timestamp.
- **`drizzle-kit generate` splits statements on a bare `;`, including inside a string literal.**
- **Zod is the gate; the CHECK is the backstop.**
- **A raw NUL byte turns a source file binary and every tool then lies to you** — `grep -rn` returns nothing for that file and `git diff` says "Binary files differ" even with `--text`. The Edit tool round-trips one faithfully, so it recurs. **Detect with `od -c` or `grep -clP '\x00'`, never plain `grep`.**
- **Never restore a mutation with `str.replace("", ...)`.** An empty search string matches at position 0, so it PREPENDS rather than inserts and silently corrupts the file. Task 1 did this to `row-link.ts` and caught it only because the result would not parse. Restore by re-editing the exact text, or by keeping a copy of the original.
- **Do not `git checkout -- <file>` to undo a mutation experiment in a file you also edited this round** — it reverts to HEAD and silently discards your real work.
- **A version-specific representation is not a property.** Assert what a thing means, not how one version encodes it.
- **Comments must be true.** If you write a number, measure it. This project has corrected
  false measured numbers **nineteen** times as of Task 3.

  **THIS TOTAL IS THE MOST STALE-PRONE LINE IN THE FILE. It has now gone stale three times,
  and once WITHIN A SINGLE TASK** -- I wrote "sixteen" here and Task 2's own correction round
  produced the seventeenth an hour later. **Task 3 then shipped a plan entry asserting the
  total "stands at seventeen" in the very commit that contained its own eighteenth and
  nineteenth** -- the check was run, honestly, and then the correction round found two more
  in what it had just checked. Each task records its ordinal beside its own bullet and this
  line then has to be moved to agree, so it is always the last thing updated and the first
  thing wrong. If you correct a number, update BOTH.

  **Do not quote this total without checking it.** Read the HIGHEST ordinal recorded -- the
  ordinals name which correction a task made, so the highest one is the total and counting
  them is not (this file holds five of them and the total is nineteen):

  ```
  grep -in teenth docs/superpowers/plans/2026-08-30-conduit-v1.2.0-accessibility.md
  ```

  `teenth` is the instrument because it is case-insensitive against a file that spells
  `fifteenth` in lower case and `SIXTEENTH` and `SEVENTEENTH` in upper, and because it does
  not collide with `fifteen lines apart` further down. **The first version of this bullet
  prescribed grepping for the two ordinals literally, which found one of the two** -- a
  convention whose own instrument is broken is worse than no convention.

  The lesson under the tally is not the tally. Task 1's review corrected four further figures
  INSIDE its own correction -- a margin, an x-coordinate, a pixel delta and a commit message
  -- and Task 2's first round then did it again. **The first measurement of a thing is usually
  wrong**, so measure it where the reader can check you, and expect your correction to need
  correcting.
- **A COMMIT MESSAGE IS A COMMENT THAT CANNOT BE EDITED.** Tasks 1 and 2 each shipped a false
  number in a message while the comments in the same commit had it right -- `1a07a3e`
  attributed a wide fixture's reading to a short one, and `4f0aa24` claimed two mutation
  survivors where one exists. Nothing can repair either; both corrections live in this plan
  naming the commit. **Measure before you write a number in a message**, and prefer pointing
  at the file that carries the measurement over restating it.
- **A guard that reads source must be scoped to the construct it guards.** Task 2's first
  draft matched `event.preventDefault()` file-wide, and that string appears **six** times in
  `composer.tsx`, so the guard passed with the handler deleted. Task 2 then did it AGAIN in the
  commit that fixed it -- `container.focus();` is spelled twice in that file, so the assertion
  naming the new null-handle branch stayed green when that branch was deleted, with typecheck
  clean. **Task 1 hit this class twice** (an override and a misplacement); its third gap was a
  different animal -- an e2e COVERAGE hole, where deleting `ROW_LINK_ROW` left every browser
  test passing.

  Mutation-test every source-reading guard by deleting the thing it claims to protect. And
  **narrow the claim rather than widening the match**: such a guard stops removal by OMISSION,
  and cannot stop tampering. A rename or a misplacement is caught by `npm run typecheck`, not
  by the guard; a trailing `//` comment or a string literal satisfies it; `if (false as
  boolean)` walks straight past it. Say so in the file instead of chasing it.
- **Tailwind v4 scans class names in prose**, including in `.md` and config files under `packages/web` — a class in a comment is emitted as CSS and moves the stylesheet hash. `lib.test.ts`'s guard catches hyphenated tokens only; bare English words are uncatchable, so **measure the hash**.
- **`documentElement.scrollWidth` is blind to page-content overflow** and has been since the first web commit, at every viewport — a scroll container does not propagate, and neither does a clip. Measure `main`, and **prove the instrument** by injecting an over-wide probe before trusting a reading.
- Vitest from the repo root. `CONDUIT_REMOTE_DIR=/home/chris/conduit-phase4 ./scripts/remote.sh '<cmd>'`, EXPLICIT on every server call. Playwright runs locally via the hybrid loop (Phase 7 Task 6's DONE block). ASCII only. `git status` before every push. Targeted `git add`. Conventional commits, with a `Co-Authored-By: Claude <model> <noreply@anthropic.com>` trailer **naming the model that actually wrote the commit** -- this line said `Fable 5` while every commit from `46a70b3` on says `Opus 5`, and the trailer is attribution, so it follows the author rather than the plan.

---

### Task 1: Entity rows become keyboard-operable — the largest item

**The finding was recorded as a screen-reader gap and that undersells it.** The rows in every entity list — companies, contacts, projects, pipelines, my tasks — have no role, no accessible name and **no tab stop**, so nobody can open a record from the keyboard, at a desk or on a phone. Phase 6 recorded them as keyboard-inert before the responsive work and worse after it, because at phone width they also lose their table semantics.

- [ ] **Make a row what it already behaves like: a link to the record.** Navigation is what it does, so an anchor is the honest element and it brings a tab stop, Enter, middle-click and open-in-new-tab — none of which the current click handler supports. A button gives the tab stop and loses the rest.
- [ ] **The visual result must not change**, at either width.
- [ ] **The desktop table semantics must survive.** A row is a `<tr>` at desk width and a card below the breakpoint; the link has to live inside both without breaking either.
- [ ] ~~**119 e2e tests click these rows.**~~ **CORRECTED BY TASK 1, and it is the fifteenth false measured number in this project. Exactly TWO e2e tests click one of these rows**, both on companies: `crm.spec.ts:54` at 1280 and `mobile.spec.ts:169` at 390. Two further places locate a contacts row and assert its text without clicking; nothing clicked a projects, pipelines or task row at all. 119 is the size of the whole suite. **The suite must still pass unchanged** -- that part stands -- but read the regression net as two clicks on one list, not as broad coverage. **A test that needed LOOSENING is a signal the change went too far.**
- [ ] **AND BECAUSE THE NET IS THAT THIN, the two clicks are fixture-dependent.** Playwright clicks an element's CENTRE. Shrinking the overlay to the first cell **is** caught by `crm.spec.ts:54` -- but only because that fixture's names are short. Re-measured at 1280 with one row rendered, row-relative x: `Acme <runid>` (13 chars) ends its first cell at **428** against a centre of **503**, so the centre lands on the cell and the click does not navigate; **25 characters** already pushes the cell to 534 and the mutation goes unseen; this task's deliberately wide fixture reaches 715. So the margin is **75px of fixture length, not a guard**. `e2e/row-links.spec.ts` (added by Task 1) does not depend on it: its fixture is wide enough that the centre falls inside the name cell, it asserts that before probing, and it probes the row's far edges. **A CORRECTION TO `1a07a3e`'s COMMIT MESSAGE**, which claimed the centre still answers the anchor under the mutation "so crm.spec.ts passes on a mutation that breaks most of the row": that is false and was the wide fixture's reading attributed to the short one. The comments that commit landed in `row-links.spec.ts` and `row-link.ts` had it right; only the message was wrong, and a pushed message cannot be edited, so the correction lives here.
- [ ] Verify with the keyboard, not by reading: tab to a row and press Enter, at 1280 and 390, on at least two lists.

---

### Task 2: The composer opens with the wrong thing focused

Open a new mail and focus is on Close, so the first act is either a mouse reach or tabbing past a control that discards the draft.

- [ ] **COORDINATOR RULING, flagged for objection: change it at both widths.** Phase 6 deferred this because the first tabbable element on desktop is the From `SelectTrigger`, so fixing the phone changes the desk — and v0.10.0 forbade altering the desktop. That rule existed to stop a responsive sweep silently degrading the desk, **not to freeze a defect**.
- [ ] **A new compose focuses To. A reply focuses the body**, since the recipient is already known. Those are different assertions and both are the point.
- [ ] Assert at both widths. v1.1.0 established that a focus bug's failure mode differs between them — the same mutation gave typeahead capture at 1280 and silent loss at 390.

#### TASK 2 DONE -- three wrong elements, not one, and four corrections to my own record

Commits `4f0aa24` and `46fd6b3`. The runtime change survived two adversarial reviews
unchanged; four claims *about* it did not, and one of those was the coordinator's.

**1. A CORRECTION TO `4f0aa24`'s COMMIT MESSAGE, and it is the SIXTEENTH false measured
number in this project and the second consecutive one in a commit message.** That message
said mutation A (deleting `onOpenAutoFocus` from the composer's `DialogContent`) "fails 7
of the 9 focus assertions and the two that survive are the desk blank compose and the
forward at 1280, with no mailbox". **There is exactly ONE survivor, not two.** The forward
assertion does not live where I measured it: it is `e2e/mail.spec.ts:1514`, inside the Mail
journey, which creates a real Dovecot account and sends through it eleven lines later -- so
that composer renders the From `SelectTrigger` and Radix focuses it. I had measured a local
*mirror* of that assertion against a database with no mailbox and reported the mirror's
answer as the shipped test's. Two reviewers caught it independently by reading the fixture;
neither could execute Playwright, so it was re-run.

**RE-RUN FOR REAL**, mutation A on the shipped suite, CI run **33308943883** (throwaway
branch, deleted): **5 failed, 103 passed, 24 did not run.** What that run shows directly:

| assertion | result |
|---|---|
| `composer-focus.spec.ts:125` desk blank compose (no mailbox) | **passed** -- the one survivor |
| `composer-focus.spec.ts:133` desk record rail, To seeded | failed |
| `composer-focus.spec.ts:203` desk blank compose, mailbox present | failed |
| `composer-focus.spec.ts:317` phone blank compose | failed |
| `mail.spec.ts:1023` reply, 1280, real Dovecot account | failed |
| `mobile.spec.ts:1814` forward, 390 | failed |
| both `composer-focus.test.ts` source guards (unit job) | failed |

**THE 24 THAT "DID NOT RUN" ARE A PROPERTY OF `describe.serial`, AND IT MEANS NO SINGLE
RUN CAN ENUMERATE THESE.** A failure aborts the rest of its group, so the phone rail test,
`mail.spec.ts`'s signature test and its forward assertion, and `mobile.spec.ts`'s reply
assertion were never reached -- and the reply and forward assertions at 390 are in the *same
test*, so no filter separates them either. Those four were measured locally instead: the
phone rail test in isolation, and the forward at 1280 against a mailbox created through the
API seconds beforehand, which is the fixture `mail.spec.ts` has. All four fail. **So: one
survivor, and it is the one `e2e/composer-focus.spec.ts`'s header already names.**

**2. THE COORDINATOR'S RULING CARRIED A FALSE ARGUMENT, and I propagated it into three
files.** The ruling said the plan's "a new compose focuses To, a reply focuses the body"
misplaces a FORWARD, because a forward has a quoted body and no recipient. It does not.
This codebase's reply-versus-new discriminator is the seed's `threadId` --
`composer.tsx` titles the dialog `threadId === undefined ? "New message" : "Reply"` -- and
`conversation.tsx`'s `openForward` sets none, so a forward IS a new compose and the plan's
rule reaches **To**, which is right. What the plan's rule actually misses is **row 2, a
record's Mail tab**: also a new compose, but To arrives already holding the contact's
address. That makes it wrong on an *instance of one of its own two named cases*, which is a
smaller and truer claim. Mutation-tested: `threadId === undefined ? "to" : "body"` fails
**two** of the six unit cases and passes the forward -- see the correction below for why that
number is not three. **The ruling's table is unchanged and
the implementation always matched it; only the reasoning was wrong.**

**3. `composer.tsx` STATED A RADIX MECHANISM THAT RADIX CONTRADICTS.** It said an
`autoFocus` "would be overwritten rather than obeyed".
`@radix-ui/react-focus-scope/dist/index.mjs:80` gates the entire mount block on
`if (!container.contains(document.activeElement))`: focus already inside means
`AUTOFOCUS_ON_MOUNT` is never dispatched and `focusFirst` never runs. `ui/dialog.tsx:175-178`
had this right all along, so the repo held two comments making opposite claims about one
library. `preventDefault()` **is** required, for a different reason: focus is placed inside
the handler, which runs *after* that gate has passed, and the body target parks on the
Content container, which is not a tabbable descendant and would lose to `focusFirst`
outright.

**4. AND THE TRIM RATIONALE CONTRADICTED ITSELF INSIDE ONE PARAGRAPH.** `"Re: ".trim()` is
`"Re:"`, not `""`, so a bare reply prefix answers the same with the trim or without it. The
trim is for a whitespace-only seed and nothing else.

**WHAT CHANGED IN BEHAVIOUR, all of it small and all of it asked for.** The forward is now
asserted at 390 as well as 1280 -- it was the one row of the ruling's table with a single
width, and 390 is where its old failure was worst (focus on the control that discards the
draft). A null handle can no longer send focus outside the modal: `preventDefault()`
disables both of Radix's fallbacks, so every branch now ends inside the Content, and the
body drain no longer spends its pending flag before it has an editor to spend it on.

**TWO GUARDS ADDED, AND ONE OF THEM I HAD CALLED IMPOSSIBLE.**
`packages/web/src/components/mail/composer-focus.test.ts` reads the source -- no browser, no
database, no network. Its own first draft was weak and the mutation caught it:
`event.preventDefault()` appears **six** times in `composer.tsx`, so a file-wide match
passed with the handler deleted. Scoped now.

The second is `rich-text.tsx`'s `updateSelection: false`, which I reported as guarded only
jointly with `focus("start")` "because no journey builds two sendable accounts" -- while the
same commit contained the fixture that builds one. A reviewer's design (two accounts, the
second signed; type, switch, type again) works, and now ships. **Two things had to be
measured before it did**: closing a Radix Select takes focus out of the editor entirely
(`document.activeElement` is a div and the DOM selection has left the composer), and a DOM
`focus()` on the contenteditable *destroys the selection under test* -- the browser picks
offset 0 and ProseMirror reads that back into its state, so the test fails with the fix in
place. The gesture that works is the toolbar's **Bold** button: the app's own
restore-the-caret path, and a different call site from `RichTextHandle.focus()`, which is
what keeps the two halves separable. `focus("start")` remains dead-equivalent today and its
comment now says exactly that instead of claiming the two diverge.

**TWO SUSPICIONS I WAS GIVEN, BOTH REFUTED BY REVIEW, both worth a line so they are not
re-raised.** A reply seeds **no body at all** (`openReply` sets `bodyHtml` nowhere; only a
forward quotes an original, and a forward focuses To), so `focus("start")` can never land
inside a quoted original. And opening the composer then immediately closing it leaks
nothing.

**A TEST RACE OF MY OWN, found by these mutations rather than by CI.** The record rail
builds its seed at click time from `useContact`, so clicking Compose before that query
resolved gave a blank compose -- which focuses To, and looks exactly like a pass of the
wrong assertion. Both rail tests now wait for the record's heading first.

**A CROSS-SPEC FIXTURE DEPENDENCY, written down rather than engineered away.**
`mail.spec.ts`'s `beforeAll` archives every non-archived own account unconditionally and
never archives its own at the end, so `composer-focus.spec.ts`'s four account-free tests are
account-free *by alphabetical file order and `workers: 1`*. That holds in CI and does not
hold in a local parallel run; the spec's header now says so.

**RECORDED IN THE BACKLOG, NOT FIXED:** a reply's signature is silently lost on a cold
accounts cache -- `composer.tsx` stamps its guard key before looking the signature up, and
the reply path is precisely the one that seeds an account id while `sendableAccounts` is
still empty. It interacts with this task in both directions; the backlog entry says how.

**A THIRD CORRECTION ROUND FOUND FOUR MORE, AND TWO OF THEM ARE IN `46fd6b3`'s MESSAGE,
which cannot be edited once pushed.**

**"Fails three of the six unit cases" IS TWO, AND IT IS THE SEVENTEENTH FALSE MEASURED
NUMBER.** It appears in `46fd6b3`'s message and appeared in `mail-lib.ts`. Re-measured
against the fixtures as they now stand: the mutated reply-versus-new rule fails the
record's Mail tab and the bare-prefix case, and nothing else. **The cause is that same
commit**: it added an explicit `threadId` to the reply fixture -- to record that
`composerInitialFocus` never reads one -- and a rule that reads *only* `threadId` therefore
gets the reply right. Against `4f0aa24`'s fixtures three was correct. This is precisely the
failure the Conventions block predicts, restating a measurement that one's own test change
invalidated, and it arrived a task earlier than that bullet expected.

**"EXACTLY ONE E2E TEST SEPARATES THE NEW DESKTOP BEHAVIOUR FROM THE OLD ONE" IS FALSE, and
the evidence against it is the probe run cited above.** It appears in `46fd6b3`'s message
and appeared in `composer-focus.test.ts`. There are **two**: the blank compose at 1280 with
the unreachable-mailbox fixture, and `composer-focus.spec.ts:128`, a record's Mail tab at
1280 **with no mailbox at all** -- its To arrives holding a chip, and the chip's Remove
button is what the old code focused. That test failed under mutation A in run 33308943883
with an empty `mail_accounts` table, and `composer-focus.spec.ts`'s own header said "the
chip button at 1280" in the same commit that got this wrong. **This is better news than the
claim it replaces**: the "coverage of exactly one" worry that motivated the source-reading
guard was already answered by the evidence, so that file is kept for a smaller and true
reason -- it is the only guard here that cannot flake, and it names the mechanism rather
than its consequences.

**THE SOURCE-READING GUARD HAD A HOLE OF EXACTLY THE KIND IT HAD JUST FIXED.** Its
null-handle assertion matched `container.focus();` file-wide, and that string is spelled
twice -- in the null-handle branch and in `focusInitial`'s body branch. Deleting precisely
the branch the assertion is named after left it green and typecheck clean. Scoped now,
the same way the `preventDefault` assertion was scoped after that one turned out to match
six times. **And the guard's claim is now stated narrowly rather than widened**: it prevents
removal BY OMISSION only. `npm run typecheck` covers a renamed prop or a handler moved to an
element that cannot take it; a spelling in a string literal or after a TRAILING `//` comment
satisfies it (`test/source.ts` strips block comments and line-leading `//` only, deliberately);
`if (false as boolean)` passes; and reverting the pending-flag fix in the drain effect is
invisible to it and to typecheck alike. Task 1 reached the same place and the answer was the
same: narrow the claim rather than widen the match.

**AND `rich-text.tsx` CONTRADICTED ITSELF FIFTEEN LINES APART, in the same commit.**
`focus()`'s comment still said "reverting only the option passes", which `appendAtEnd`'s new
comment and the commit message both contradict -- the account-switch journey added in that
commit catches it alone. That is the same defect removed from `composer.tsx` one round
earlier. The redundancy is one-sided and now says so: the option defends itself,
`focus("start")` does not, and it stays only because stating the caret's position costs a
word.

**Minor, folded in:** `e2e/mobile.spec.ts:1569` also creates a mail account and also sweeps
every live own account at its start, so three spec files overlap in a local parallel run,
not two.

**RECORDED IN THE BACKLOG, NOT FIXED:** a residual of the rail race this task closed. The
same `MailRail` opened from a **deal** or **project** tab resolves its contact through
`deal?.contactId` -- a two-deep query chain -- and nothing waits on it. Composing before
both settle seeds no recipient at all. The contact tab, which this task's tests use, is one
deep and now waits on the record heading.

**Baseline moved to 2460 unit / 38 skipped locally (2455 + 5 source guards) and 132 e2e**
(125 + 6 in `composer-focus.spec.ts` + 1 signature journey in `mail.spec.ts`). **The forward
assertions are not part of the +7**: both went inside existing tests, at 1280 in
`mail.spec.ts`'s forward journey and at 390 in `mobile.spec.ts`'s phone conversation. The
six in the new file are two desk, two mailbox and two phone; the account-switch journey is
one of the mailbox pair. Stylesheet hash unmoved at `index-M5teG3jg.css`, 33259 bytes,
measured before and after all three commits.

---

### Task 3: Closing a dialog leaves focus on `<body>`

Pre-existing, visible at a desk as well as a phone, affecting the task drawer, the composer and both settings dialogs.

- [ ] **Find the cause before fixing it.** Radix restores focus to the trigger by default, so something is overriding or unmounting it. Do not focus something plausible on close and call it done — a workaround will paper over whatever is also causing it elsewhere.
- [ ] Note that v1.1.0 added `onCloseAutoFocus` forwarding to `ui/select.tsx` for the contact pickers. That is a Select, not a Dialog, but it is the same family of problem and worth reading first.
- [ ] Assert focus lands on the trigger, not merely that it is not `<body>`.

#### TASK 3 DONE -- the cause was right, the population was not, and three designs were measured failing

Commits `6addd77`, `c29cb93` (both superseded in substance by the correction round below)
and the correction commit. The cause `pages/board.tsx` recorded is confirmed exactly:
`DialogContentModal` composes `onCloseAutoFocus` as `event.preventDefault()` then
`context.triggerRef.current?.focus()` (`@radix-ui/react-dialog dist/index.mjs:154-157`),
that ref is written only by `<DialogTrigger>`, and the `preventDefault()` has already
disabled FocusScope's own restore (`react-focus-scope dist/index.mjs:99`).

**SIX DIALOGS USE THE SHARED HOOK; SEVEN MORE LOSE THE CARET AND ARE SCOPED OUT IN
WRITING.** Sixteen `<Dialog>` roots across twelve files were measured at 1280 and 390.
Five landed on `<body>`: the four the finding named, plus `pages/deal-detail.tsx`'s
**Lose** dialog, which HAS a trigger and fails because a successful lose unmounts it while
the same dialog dismissed restores correctly. The sixth caller is board's move sheet,
migrated off its hand-rolled copy.

**The other ten restore their trigger on a DISMISSAL, and seven of them lose the caret on
their SUCCESS path** -- the create dialogs on companies, contacts, projects, pipelines, a
company's contacts and pipelines, and a project's pipelines all `navigate()` in
`onSuccess`, which unmounts the page the trigger was on. All seven measured `<body>`.
**They are deliberately not fixed**, and the argument is a measurement rather than a
preference: clicking an ordinary company ROW LINK, with no dialog within reach, lands on
`<body>` for the identical reason, while a SIDEBAR link (whose anchor survives) keeps
focus. The rule is "any navigation that unmounts the focused element", row links are far
the commoner path to those same pages, and fixing seven dialogs would make them the only
navigations in the app that land anywhere. Recorded in the backlog as one routing item;
each of the five roots says so in place.

**THE EIGHTEENTH AND NINETEENTH FALSE MEASURED NUMBERS, both mine, both in `6addd77`.**

**EIGHTEENTH: "the seven working ones".** `e2e/dialog-focus.spec.ts` said seven in one
paragraph and ten in the one above it. Ten is right.

**NINETEENTH: "ten roots need nothing at all", in `6addd77`'S MESSAGE and in three
files.** Seven of the ten lose the caret on their success path, measured above. The claim
was generalised from measuring DISMISSALS on all eleven trigger-driven roots and the
success path on Lose alone -- exactly the shape of error the Conventions block warns
about, and it went into a commit message that cannot be edited. `c29cb93`'s message
additionally asserts the tally "stands at seventeen"; it did not, in that same commit.

**AND A FALSE MEASUREMENT THAT IS NOT A NUMBER, so it is recorded rather than counted.**
`6addd77` claimed board's Move button was "instrumented at the macrotask
`onCloseAutoFocus` actually runs in" and read `isConnected` true. It does not. That probe
registered its `setTimeout(0)` BEFORE the click, so it fired a commit earlier than the one
Radix restores focus in. Mutation-testing settled it: deleting board's `forget()` leaves
all three phone-kanban tests green, because `useMoveDeal` is optimistic and the card is
gone before the close. `pages/deal-detail.tsx` is the real case -- `useLoseDeal` only
invalidates -- and deleting ITS `forget()` turns two tests red. Both comments now say
which is which, and board's says outright that its line is redundant and untested.

**THE CAPTURE MOVED OUT OF THE RENDER PHASE, on the coordinator's ruling, and the design
it replaced was a real bug rather than a style point.** `src/hooks.ts`'s `useLatest`
forbids writing a ref during render in as many words, and `pages/inbox.tsx` restates it as
a rule. The version shipped in `6addd77` did exactly that, and worse than the case the
rule was written for: the flag was an EDGE DETECTOR, so a render React throws away
consumes the edge permanently and the capture never happens -- silently, and looking
exactly like the deep-link case. It also cited React's docs for "adjusting a ref while
rendering", which document adjusting STATE; the `useRef` page carries the opposite
instruction.

**THREE CAPTURE POINTS, TWO MEASURED FAILING, and all three are written into
`components/ui/dialog-focus.ts`:**

1. **Radix's `onOpenAutoFocus`** -- never dispatched when the content marks a field
   `autoFocus`, because FocusScope gates its mount block on
   `!container.contains(document.activeElement)` (`dist/index.mjs:80`). Three of the six
   callers do, and all three closed onto the fallback -- a REGRESSION for Lose, which
   Radix had been getting right.
2. **`document.activeElement` on the render where `open` turns true** -- the render-phase
   bug above, and browser-dependent besides: `activeElement` is the opener only where a
   click focuses a `<button>`, which is why board had passed the element explicitly since
   v1.1.0. Migrating board onto the reading version silently made `e2e/mobile.spec.ts:810`
   browser-dependent; that regression is undone.
3. **`event.currentTarget`, from the handler that opens the dialog** -- what ships. Not in
   the render phase, before any `autoFocus`, and indifferent to how a browser treats focus
   on click.

**THE COST IS PLUMBING, AND IT IS THE POINT.** `TaskDrawer` and `Composer` take
`returnFocus` as a REQUIRED prop, so a new mount site is a type error rather than a silent
landing on `<main>`; seven mount sites and nine openers were updated, including the gantt,
whose `onOpenTask` now carries the bar for the keyboard path and `null` for its two
`aria-hidden` phone tap overlays -- an element no caret should land on is worse than none.

**THE TASK DRAWER'S OWN ACTIONS.** `handleArchive` and `handleStatusChange` retire the
card while the drawer stays open, and neither mutation is optimistic, so closing inside
the round trip was measured landing on the card and then on `<body>`. Both call `forget()`
now, and `returnFocus` reaches `TaskDrawerBody` for it.

**`deal-detail` PASSES ITS OWN `<h1>`** rather than the shared `<main>`: on the one path
that reaches the fallback the user has just changed the deal's status, the badge is the
heading's sibling, and nothing else on that page announces it. `tabIndex={-1}` at both
widths, unlike inbox's and board's phone-only headings.

**THREE MORE FALSE CLAIMS, all corrected in place, none a number.** `ui/dialog.tsx` said a
`node`-environment unit test cannot import a `.tsx`; measured false -- a probe importing
`ui/dialog` and `mail/composer` passes. The real constraints are that nothing here can be
RENDERED and that the include glob does not match `.test.tsx`. `shell.tsx` said
`tabIndex={-1}` on `<main>` changes nothing else; measured false -- a click on plain
content inside it now leaves `activeElement` on MAIN where it left `<body>`. And the
citation `ui/dialog.tsx:175-178` in `composer.tsx` and the plan moved to **189-192**,
displaced by this task's own insertion into that file.

**FOURTEEN MUTATIONS.** Twelve ran the e2e file per-title (a `describe.serial` failure
aborts the rest of its group, so one run cannot enumerate what a mutation kills); two ran
the unit file. Out of **18** e2e cases -- nine titles at two widths:

| mutation | e2e cases killed |
|---|---|
| `capture` made a no-op | 12 -- every "goes back to" case; the three fallback cases survive |
| `event.preventDefault()` in `restore` | 2 -- the lost-deal pair only |
| `tabIndex={-1}` off `<main>` | 4 -- deep link and archived-task pairs |
| `tabIndex={-1}` off the deal `<h1>` | 2 -- the lost-deal pair |
| `forget()` in deal-detail's onClose | 2 -- the lost-deal pair |
| `forget()` in the drawer's handleArchive | 2 -- the archived-task pair |
| composer + settings-mail `onCloseAutoFocus` | 4 -- their own, nothing else |
| task-drawer + settings-templates `onCloseAutoFocus` | 6 -- including the deep-link pair |
| deal-detail `onCloseAutoFocus` | 2 -- lost-deal only; the DISMISSAL survives, correctly, because removing it hands that exit back to Radix, which was always right about it |
| the inbox's `capture` call | 2 -- the composer pair |
| settings-templates' row `onEdit` capture | 2 -- the two-rows pair |
| `mainLandmark()` returning null | 4 -- deep link and archived-task pairs |

And against `e2e/mobile.spec.ts`'s phone kanban group: removing board's
`onCloseAutoFocus` kills `:771` (`:810` could not be reached, because its fixture is built
by `:771`); removing board's `forget()` **kills nothing**, which is the correction above.
Unit: dropping `isConnected` kills 2, dropping the null check kills 1.

**ONE LINE IS KNOWINGLY UNCOVERED AND SAYS SO**: `restore`'s `triggerRef.current = null`.
Every path that opens a dialog without capturing arrives after a navigation, which
detaches the previous trigger anyway, so `isConnected` already refuses it. It is kept to
keep the invariant local, and its real hazard -- a reopen inside the closing macrotask
losing its own capture -- is written down instead of the tidier reason it used to give.

**NO SOURCE-READING GUARD WAS ADDED**, deliberately: every line of the fix is exercised
end to end at both widths by tests shown failing without it.

**Baseline: 2464 unit** (2460 + 4 in `components/ui/dialog-focus.test.ts`) **and 150 e2e**
(132 + 18 in `e2e/dialog-focus.spec.ts`). Stylesheet unmoved at `index-M5teG3jg.css`,
33259 bytes, measured before and after.

**AN INTERMITTENT SIGHTED, NOT MINE.** `e2e/mobile.spec.ts:733`'s `addStage` failed once
in three isolated runs of its group and twice passed. It is an inline form, not a
`<Dialog>`, so nothing in this task can reach it; it is the same `addStage` Task 4's
related-work bullet already tracks.

**RECORDED, NOT FIXED.** Six readings in
`docs/superpowers/plans/2026-08-27-conduit-phase-6-responsive.md` (`:211`, `:280`, `:342`,
`:517`, `:572`, `:626`) are bare "activeElement was BODY" with no instrument stated, and
`:517` is the reading that seeded this task. This task's `setTimeout(0)` discovery makes
all six unreliable as written -- a reading taken the moment a dialog disappears answers
BODY for every dialog in the app, working ones included. **The conclusions survive
independently** (a null `triggerRef` focuses nothing, deterministically, and the capture
mutation kills 12 of 18 cases), but nothing in that file records how it was measured.

Also recorded: `deal-detail.tsx`'s New quote trigger is `disabled={archived}`, so an SSE
update or another tab archiving the deal mid-dialog would leave Radix focusing a
connected-but-disabled button. That is a hole in the ten-need-nothing set, and the same
hole `dialogReturnTarget` names in itself.

---

### Task 4: The intermittent, which is now reproducible

Four sightings across two phases, **never once failing CI**. Phase 7 measured **1 failure in 12 runs idle, 8 in 12 with a second vitest process running** — contention amplifies a real race rather than causing it.

- [ ] **Two hypotheses are already dead and both are recorded**: it is not `waitFor`'s 10s deadline (vitest's own 5000ms fires first, so that label can never appear) and it is not a slowdown (the case costs 180-240ms and instead **wedges**).
- [ ] **Named next suspect, untested:** `ManualClock.wait(ms <= 0)` resolving without registering a pending entry, so a `waitFor(() => clock.pendingCount() > 0)` waits for something that has already happened.
- [ ] **The fix must be deterministic, not a longer timeout.** That file owns a `ManualClock`, and a wall-clock deadline inside a test that controls its own clock is the defect. If the named suspect is wrong, **say so and report what it is instead** — a second falsified hypothesis with evidence is a good outcome.
- [ ] **Two more intermittents surfaced during v1.1.0 and are related work, not yours unless they are the same bug**: `mail-integration.test.ts`'s Dovecot IDLE burst (17 of 20 messages), and `e2e/mobile.spec.ts`'s phone kanban `addStage` (once in eight runs; note `board.tsx:613` put a sticky strip directly above that button in v1.1.0, so "pre-existing" is not established). Say whether any share a mechanism.

---

### Task 4b: the touch-floor assertions are blind to clipping

Folded in from the backlog because it is this phase's shape: an interaction correctness
gap that reads as coverage.

- [ ] `documents.spec.ts:647,664,711,747,758,763,765` assert
      `boundingBox().height >= TOUCH_FLOOR_PX`. **A bounding box exists whether or not the
      control is on screen**, so with `<main>` clipping below the breakpoint a control
      clipped entirely off-screen still passes. The assertion that was meant to cover that
      was one of the three vacuous overflow checks v1.1.0 found.
- [ ] Assert the control is actually reachable, not merely sized — `toBeInViewport()` is
      stronger under a clip (IntersectionObserver respects ancestor clipping) and is the
      obvious instrument. Whatever you choose, **prove it**: a control moved off-screen must
      fail it.
- [ ] `touch-floors.test.ts` is a source-reading guard and is a different question — it
      pins that the classes are present. Leave it; the gap is the e2e half.

---

### Task 5: e2e and the v1.2.0 release

- [ ] Keyboard journeys for Task 1 at both widths; the composer's focus for a new compose and a reply; focus after a dialog closes asserted against the trigger.
- [ ] **The existing 119 must pass unchanged.** Verify from the diff. **Task 1 added `e2e/row-links.spec.ts` (5 tests, so 124 in 10 files)** and changed no executable line of any pre-existing spec; see the corrected click count under Task 1 for why that file exists.
- [ ] Release prep **1.2.0** across three package.jsons, `manifest.toml` (`1.2.0~ynh1`), **and the lockfile** — v1.0.1 bumped the packages and left the lockfile at 1.0.0, which nobody noticed for two releases. Leave `resources.sources.main` at v1.1.0.
- [ ] The sequence reuses v1.1.0's shape: the `--branch v1.2.0` run filter, the filename-anchored digest grep, the non-optional re-download-and-re-hash, the explicit `--notes-file`, step 1 detaching onto `origin/main` and pushing `HEAD:main`, no `git`/`gh` from a `mktemp` directory, `sudo` on every `yunohost` call with **`-u https://github.com/chriswilson2020/conduit_ynh`**, a close-out whose branch-delete gate is chained with `&&`, and a manifest snippet of **two lines, `url` and `sha256` only**.

  **`autoupdate.strategy` was REMOVED in `357b800` and must not come back.** v1.1.0's sheet showed a four-line block; that line was inert (this app is not catalogued, and the id `conduit` belongs to a Matrix homeserver) and would have repointed installs at GitHub's source archive if it ever fired. `manifest.toml` now carries the reasoning where it used to sit. Copying v1.1.0's snippet verbatim would restore it.
- [ ] **Do NOT merge, tag or publish.** The coordinator gates.
